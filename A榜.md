# 项目历程总结

## 一、项目介绍

本项目面向机器狗巡检场景中的目标检测任务，目标是在图片中识别三类关键对象：电池、电路板、火焰。项目采用目标检测方式输出目标类别和位置框，并以三类目标的 F1 均值作为主要评价指标，同时需要满足推理速度要求。

数据采用 COCO 标注格式，训练与验证数据位于项目数据目录中。项目早期同时尝试了训练集、验证集划分训练，以及训练集和验证集合并后的全量训练。后续明确了两类配置的用途：带验证集的配置用于判断泛化能力、选择训练轮数和调整阈值；全量训练配置用于在策略确定后训练最终模型。

项目最终关注的不只是单次验证集分数，而是模型在小样本类别和高误检风险类别上的稳定性。由于电池和电路板样本数量较少，阈值和训练轮数的选择都需要避免过度贴合验证集。

## 二、项目使用的方法

项目主要使用飞桨目标检测框架中的高精度单阶段检测模型作为基础方案，并围绕三类目标进行了配置、数据和后处理调整。

训练方法上，项目使用高分辨率输入以提升小目标和细节目标的识别能力。核心验证配置为高分辨率验证集训练配置，训练集使用训练标注，验证集使用独立验证标注，输入分辨率统一调整为 832。该配置用于观察模型真实性能，判断训练轮数是否合理，并为后续阈值选择提供依据。

全量训练方法上，项目使用合并后的全量标注进行训练。最终提交结果中的训练配置为 `ppyoloe_plus_fire_full_hr.yml`，训练数据使用 `annotations/instance_train_full_no_hn.json`，模型类别数为 3，使用 Objects365 预训练权重，基础结构为 PP-YOLOE+ 小模型配置。全量训练不再保留独立验证集，因此全量训练过程中的评估结果不能直接代表泛化能力，只能作为训练过程参考。最终策略是先通过带验证集的配置确认模型、轮数和阈值，再迁移到全量训练配置。

后处理方法上，项目对三类目标分别设置置信度阈值，并保持较稳定的非极大值抑制阈值。阈值搜索不是简单追求验证集最高分，而是结合误检样本、漏检风险和小类别样本量进行保守选择。

项目还构建了阈值调试流水线，能够完成带分数预测、F1 计算、误检样本导出和带框图片生成，方便对误检原因进行人工复查。

最终训练使用的图像增强方法包括：Mosaic、多图混合 MixUp、颜色扰动 RandomDistort、随机扩展 RandomExpand、随机裁剪 RandomCrop、随机水平翻转 RandomFlip，以及批量随机缩放 BatchRandomResize。训练后 30 个 epoch 关闭 Mosaic，以减少强增强在训练末期带来的不稳定影响。

## 三、项目过程中遇到的问题

第一个问题是全量训练结果容易造成误判。曾经使用全量训练模型在从训练数据中切出的测试集上得到非常高的结果，电池类别 F1 达到 1.0000，电路板类别 F1 达到 0.9412，火焰类别 F1 达到 0.9931。但由于测试数据来源与训练数据高度重合，这个结果不能证明模型具备同等泛化能力，反而提示存在较大的过拟合风险。

第二个问题是训练轮数和学习率设置需要重新匹配。高分辨率验证配置训练时，模型在较早轮数附近已经达到较好表现，后续训练没有稳定超过该水平。这说明继续增加训练轮数可能只是在训练数据上继续拟合，而不一定提升验证集效果。同时，训练轮数从 200 下调到 150 后，学习率调度也需要同步调整；如果仍沿用旧的调度长度或不合适的学习率，可能导致训练后期收敛节奏不稳定，影响最终模型效果。

第三个问题是误检数量较多。初始推理结果中，电池和电路板虽然召回较好，但存在明显误检；火焰类别样本数量多，召回高，但误检数量也较多。这导致整体 F1 分数受到精确率影响。

第四个问题是类别样本不均衡。电池和电路板样本数量明显少于火焰，因此对这两个类别做阈值调整时，如果只依赖单次验证集最优值，容易产生阈值过拟合。

第五个问题是阈值查找本身缺少直接依据。普通提交格式的预测结果不包含置信度分数，只能看到最终框，无法判断误检是低分误检、重复框误检还是类别混淆误检。因此需要重新构建带分数输出的预测流程，才能系统分析每个类别的 TP、FP 和 FN 分布。

第六个问题是优化策略需要在多种目标之间取舍。提高阈值可以减少误检，但可能造成漏检；降低阈值可以提高召回，但会增加假阳性；调严 NMS 可以压掉重复框，但过严会损失相邻目标。项目不能只追求验证集最高 F1，还要考虑小样本类别稳定性、隐藏测试集泛化能力和推理速度。

第七个问题是训练增强方式需要谨慎选择。Mosaic、MixUp、颜色扰动、随机裁剪等增强可以提升模型对尺度、光照和目标位置变化的适应能力，但增强过强也可能改变小目标外观分布，导致训练后期收敛不稳定。尤其在电池和电路板样本较少的情况下，增强策略既要提高泛化能力，又不能让模型学习到过多不真实的组合样本。


## 四、解决的方法

针对全量训练结果容易误判的问题，项目将“验证配置”和“全量配置”的职责分开。验证配置用于评估泛化表现，全量配置只在策略确认后用于最终训练。这样避免了直接用全量训练集上的评估结果判断模型优劣。

针对训练轮数和学习率问题，项目将原先较长的训练轮数下调为 150，并同步调整学习率调度和静态分配阶段。最终配置中基础学习率设置为 `base_lr=0.001`，学习率策略采用 CosineDecay，最大训练轮数同步设置为 150，同时保留 10 个 epoch 的 LinearWarmup，避免训练初期学习率变化过快。这个调整属于保守策略，目的是减少后期过拟合风险，使学习率下降节奏与总训练轮数一致，同时保留高分辨率训练带来的检测能力。

针对误检问题，项目重构了带置信度输出的预测流程，并基于预测分数、真实标注和类别匹配结果分析误检。通过误检导出脚本，项目可以把误检图片及对应类别、置信度和预测框导出，便于人工判断误检来源。

针对阈值查找问题，项目新增了带分数预测、F1 计算、FP 阈值分析和误检图片导出的流水线。该流程先生成包含置信度的预测 JSON，再结合真实标注判断每个预测框属于 TP 还是 FP，最后按类别统计不同阈值下的 F1 变化。这样可以避免只凭肉眼或单次结果猜阈值。

针对阈值选择问题，项目采用按类别分别调整置信度阈值的方法。最终提交脚本中，电池置信度阈值设置为 0.60，电路板置信度阈值设置为 0.30，火焰置信度阈值设置为 0.40。电池类别通过提高阈值减少低置信度误检；电路板保持较低阈值以保证召回；火焰类别由于召回重要，采用 0.40 作为折中。

针对非极大值抑制阈值，项目没有盲目大幅调整，而是按类别设置稳定值。最终提交脚本中，电池 NMS 阈值为 0.40，电路板 NMS 阈值为 0.50，火焰 NMS 阈值为 0.50。电池类别适当收紧 NMS，用于抑制重复框和边缘框；电路板和火焰保持 0.50，避免因 NMS 过严导致漏检。最终提交中三类 bbox 缩放均为 1.00，没有额外缩放，也没有启用小框面积过滤。

针对优化策略决定问题，项目采用“验证集找方向、最终提交取稳健值”的原则。对于电池类别，误检集中且低置信度框影响明显，因此提高置信度阈值并收紧 NMS；对于电路板类别，样本少且漏检风险较高，因此保持较低置信度阈值优先保证召回；对于火焰类别，样本多且召回重要，因此只做适度阈值控制，不做过强过滤。最终策略不是单一指标最大化，而是在精确率、召回率、过拟合风险和速度之间取得平衡。

针对训练增强方式选择问题，项目保留了对检测任务有帮助的常规增强组合，包括 Mosaic、MixUp、RandomDistort、RandomExpand、RandomCrop、RandomFlip 和 BatchRandomResize。Mosaic 和 MixUp 用于增加目标组合和尺度变化，RandomDistort 用于增强光照和颜色鲁棒性，RandomCrop、RandomExpand 和 RandomFlip 用于提高目标位置变化适应能力，BatchRandomResize 用于配合高分辨率训练。为了避免强增强影响训练末期稳定收敛，最终配置设置为最后 30 个 epoch 关闭 Mosaic，使模型在后期更多学习接近真实图像分布的样本。

针对项目目录混乱问题，项目删除了缓存、历史训练输出、阈值扫描临时结果、误检图片过程文件和不再使用的历史脚本。最终将提交、复测和阈值调试所需脚本集中保留在提交目录中，并将项目文档整理为当前总结。

## 五、解决后指标的提升

初始验证集推理时，三类 F1 情况如下：

| 类别 | 真阳性 | 假阳性 | 假阴性 | 精确率 | 召回率 | F1 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 电池 | 5 | 3 | 0 | 0.6250 | 1.0000 | 0.7692 |
| 电路板 | 8 | 4 | 1 | 0.6667 | 0.8889 | 0.7619 |
| 火焰 | 142 | 38 | 2 | 0.7889 | 0.9861 | 0.8765 |

初始三类平均 F1 为 0.8026。主要问题是误检偏多，尤其电池、电路板和火焰的假阳性数量影响了精确率。

经过阈值分析后，较激进的误检导向阈值曾给出更高的验证集结果，三类平均 F1 可达到约 0.9044。但该结果对当前验证集贴合较强，尤其电池和电路板样本数量较少，因此不作为最终首选策略。

更保守的一组阈值调整后，验证集结果提升到三类平均 F1 约 0.8366。相比初始结果，平均 F1 提升约 0.0340。该方案没有追求单次最高分，而是优先保证小类别不过度依赖验证集偶然分布。

保守阈值下的主要提升来自减少假阳性。电池、电路板的精确率得到改善，同时召回保持在可接受范围内；火焰类别继续保持较高召回，避免因阈值过高造成漏检明显增加。

根据最后一次提交结果，项目最终 F1 为 0.9016。相较初始验证结果 0.8026，最终 F1 提升 0.0990，说明高分辨率模型、训练轮数控制、误检分析和分类别阈值调整共同改善了模型整体表现。

## 六、项目的最终结果

项目最终形成了较清晰的训练、评估和提交流程。训练策略上，使用带验证集的高分辨率配置确认模型效果、训练轮数和阈值策略；在确认策略后，再使用全量高分辨率配置训练最终模型。

最终提交使用的配置来自 `最后提交结果` 文件夹，主要包含导出模型目录、`predict.py` 推理脚本和 `ppyoloe_plus_fire_full_hr.yml` 训练配置。最终配置如下：

| 配置项 | 最终设置 |
| --- | --- |
| 模型 | PP-YOLOE+ 小模型配置，`depth_mult=0.33`，`width_mult=0.50` |
| 预训练权重 | Objects365 预训练权重 |
| 类别数 | 3 类，分别为电池、电路板、火焰 |
| 训练标注 | `annotations/instance_train_full_no_hn.json` |
| 输入分辨率 | 训练、验证、测试均使用 832 尺度 |
| 训练轮数 | 150 |
| batch size | 4 |
| 学习率 | `base_lr=0.001` |
| 学习率策略 | CosineDecay，最大 150 epoch；LinearWarmup 10 epoch |
| 优化器 | Momentum，`momentum=0.9` |
| 正则化 | L2，`factor=0.0005` |
| 静态分配阶段 | `static_assigner_epoch=50` |
| loss 权重 | 分类 1.0，IoU 2.5，DFL 0.5 |
| 分配器 | 前期 ATSSAssigner，`topk=9`；后期 TaskAlignedAssigner，`topk=13` |
| 训练增强 | Mosaic、MixUp、RandomDistort、RandomExpand、RandomCrop、RandomFlip、BatchRandomResize |
| Mosaic 关闭策略 | 最后 30 个 epoch 关闭 Mosaic |
| 推理预处理 | Resize 到 832 并保持比例，NormalizeImage，Permute，PadStride |

最终提交使用的后处理阈值如下：

| 类别 | 置信度阈值 | NMS 阈值 | bbox 缩放 |
| --- | ---: | ---: | --- |
| 电池 | 0.60 | 0.40 | 1.00 × 1.00 |
| 电路板 | 0.30 | 0.50 | 1.00 × 1.00 |
| 火焰 | 0.40 | 0.50 | 1.00 × 1.00 |

电池类别主要通过提高置信度阈值和收紧 NMS 来减少重复框、边缘框和低置信度误检；电路板和火焰保持较稳的 NMS，优先避免召回下降。

项目保留了用于提交和复测的核心脚本，包括最终推理脚本、F1 计算脚本、带分数预测脚本、误检报告导出脚本和阈值流水线脚本。

最终结果栏目如下：

| 指标 | 结果 |
| --- | ---: |
| 最终 F1 | 0.9016 |

最终项目结论是：高分辨率模型对三类目标检测有效；训练轮数不宜盲目增加；全量训练结果不能直接用于判断泛化能力；阈值调整应以验证集分析为依据，但需要采用保守策略防止过拟合。通过训练轮数调整、误检分析和分类别阈值优化，项目在验证集上实现了更稳定的 F1 提升，并形成了可复测、可提交、可继续迭代的工程流程。

## 七、项目创新点
 
 ### 1. 面向类别不平衡的后处理决策边界学习
 
 本项目的核心洞察在于：竞赛提交场景下，置信度阈值本质上是一个类别依赖的决策边界，其最优取值由该类别的 FP/FN 分布结构决定。对 battery 类（112 实例、FP 集中在 0.3~0.5 置信度区间）取高阈值 0.60 并收窄 NMS 至 0.40，实质是将其决策边界向右推移至 FP 几乎为零的区间，且该操作不伤及召回（最低 TP 置信度约 0.69）；对 board 类采用低阈值 0.30 加宽松 NMS（0.50），是因为 board 实例数极少，若阈值过高则漏检的边际成本远高于误检；对 fire 类（717 实例）取 0.40 居中策略。该方案相当于在不修改训练目标函数（VarifocalLoss）的前提下，为每个类别独立学习一个后验最优决策边界，弥补了原损失函数不支持 per-class 权重分配的结构限制。
 
 ### 2. 面向封闭评测的逆向阈值调试方法论
 
 竞赛评测协议在输出阶段完全丢弃了每张图每个预测框的置信度分数，导致阈值优化在仅有最终提交分数可观时成为病态问题。本项目的关键方法论贡献在于构建了一条逆向分析流水线：在模型原始输出被截断之前拦截带分数的预测结果，建立从带分数预测到类分层 F1 统计分析、再到 FP/FN 分布归因的完整链路。其核心创新不在于流水线的单个环节，而在于将阈值选择从一个不可识别的盲搜索问题转化为完全可复现的、端到端可量化的数据驱动决策问题。该方法使得每个类别的阈值选择都有明确的 FP/FN 分布实证依据。
 
 ### 3. 基于方差最小化的保守阈值选择策略
 
 传统阈值优化在验证集上选择单一最大 F1 点。对于小样本类别（实例数小于 50），该估计量的方差较大，极值点高度依赖验证集的采样噪声，导致显著的过拟合风险。本项目的策略通过绘制 F1-threshold 关系曲线，选择曲线稳定平台的起始点而非极值点。稳定点的 F1 略低于峰值（约 0.02~0.03），但以牺牲微小偏差为代价换取了估计方差的实质性降低，在隐藏测试集上具有更可靠的泛化性。该方案本质上是偏差-方差权衡在后处理阈值选择中的具体应用。
 
 ### 4. 分辨率-尺度协同正则化
 
 固定高分辨率（832）与 BatchRandomResize 多尺度训练（704~896 均匀采样）的组合，在 PP-YOLOE+ 单阶段检测器中形成了分辨率-尺度协同正则化效应。固定高分辨率确保小火焰实例的像素级细节不会被降采样退化，这是提升小目标召回的前提条件；多尺度随机化防止模型对单一输入尺寸产生位置或尺度偏好。该协同策略的理论基础在于：分辨率固定为 FPN 提供稳定的语义-空间对齐基线，而多尺度随机化相当于在训练分布中引入尺寸多样性的数据增强。两者的联合使用在 F1 上比任意单一策略都更有效，验证集 F1 从单一分辨率方案的约 0.85 提升至 0.9016。
