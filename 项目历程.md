# 项目历程

> 本文档由 dog 目录下的项目 Markdown 文件整合生成。Agent 配置文档 `Agent.md`、`AGENTS.md`、`CLAUDE.md` 已保留为独立文件，不纳入项目历程。

## 整合来源

- `fire_postprocess_tuning_guide.md`
- `FULL_DATASET_TRAINING_GUIDE.md`
- `MODEL_OPTIMIZATION_HISTORY.md`
- `ppyoloe_plus_objects365_c1_2026-06-15.md`
- `predict_keep_ratio_retune_2026-06-15.md`
- `README.md`
- `训练结果改进调研报告.md`

---

## 来源: fire_postprocess_tuning_guide.md

# Fire Postprocess Tuning Guide

This note documents the script for tuning fire post-processing without blindly overfitting the validation set.

## Goal

Use `mycode/scripts/tune_fire_postprocess.py` to search:

- fire score threshold
- fire NMS IoU threshold
- fire small-box minimum area

The script reports both:

- `best_raw`: the highest validation F1 in the search space
- `recommended_stable`: a conservative choice near the best score

Prefer `recommended_stable` first. It penalizes overly high thresholds, overly strict NMS, large min-area filtering, and clear fire recall drops.

## Generate Raw Scored Predictions

The official submission JSON usually does not contain `score`, so it cannot be used for threshold tuning directly.

Use the real submission script to export raw scored predictions:

```bash
python mycode/scripts/tune_fire_postprocess.py --predict-py "D:/work/Marchine Dog/predict.py" --infer-txt val.txt --gt A_train/coco/annotations/instance_val.json --raw-out raw_preds_with_score.json --out fire_postprocess_tuning.json
```

By default, the model directory is resolved as `D:/work/Marchine Dog/model`. Override it when needed:

```bash
python mycode/scripts/tune_fire_postprocess.py --predict-py "D:/work/Marchine Dog/predict.py" --model-dir "D:/work/Marchine Dog/model" --infer-txt val.txt --gt A_train/coco/annotations/instance_val.json
```

## Reuse Existing Raw Predictions

If `raw_preds_with_score.json` already exists:

```bash
python mycode/scripts/tune_fire_postprocess.py --pred-json raw_preds_with_score.json --gt A_train/coco/annotations/instance_val.json --out fire_postprocess_tuning.json
```

## Practical Search Range

The defaults are intentionally small and conservative:

- `fire_thresholds`: `0.30,0.35,0.40,0.45,0.50,0.55`
- `fire_nms_values`: `0.45,0.50,0.55,0.60`
- `fire_min_areas`: `0,50,80,120,160,200`

For quick score improvement when fire FP is high, inspect candidates around:

- fire threshold: `0.40` to `0.50`
- fire NMS: `0.45` to `0.55`
- fire min area: `50` to `120`

Avoid jumping directly to extreme values unless the hidden test result confirms the trend.

## Verification

Run:

```bash
python mycode/scripts/test_tune_fire_postprocess.py
python -m py_compile mycode/scripts/tune_fire_postprocess.py mycode/scripts/test_tune_fire_postprocess.py
```

---

## 来源: FULL_DATASET_TRAINING_GUIDE.md

# 全量数据集训练与导出指南 (Train + Val 合并训练)

本文档记录了使用全量数据集（366张 Train + 81张 Val = 447张图片）进行极限微调的两种方案。
**目标**：在不切分验证集的情况下，利用所有可用数据压榨模型的最终性能。

> [!WARNING]
> 全量训练时，终端输出的 `mAP` 分数是在训练集自身上计算的，仅供监控 Loss 收敛状态使用，**严禁作为评估模型泛化能力的真实指标**。真实能力需以最终提交线上测试的结果为准。

---

## 方案 A：标准分辨率全量训练 (推荐基线)
**配置文件**: `configs/custom/ppyoloe_plus_fire_full.yml`
* **分辨率**: 多尺度 `[640, 704, 768]`
* **Batch Size**: 4
* **优势**: 训练速度快，显存占用小，模型推理 FPS 高，在小数据集上不易过拟合。

### 1. 启动训练
```bash
cd "w-yolo\Marchine Dog\dog\PaddleDetection"

conda activate dog

# (可选) 如果配置文件还没拷贝，先执行拷贝
cp "../mycode/configs/ppyoloe_plus_fire_full.yml" "configs/custom/"

# 启动训练
python tools/train.py -c configs/custom/ppyoloe_plus_fire_full.yml --eval
```

### 2. 导出模型
```bash
cd "D:\work\Marchine Dog\dog\PaddleDetection"

# 将最优 epoch 导出为静态图模型
python tools/export_model.py \
  -c configs/custom/ppyoloe_plus_fire_full.yml \
  --output_dir=../model_full_768 \
  -o use_gpu=False weights=output/ppyoloe_plus_fire_full/best_model.pdparams
```
导出的模型将位于 `D:\work\Marchine Dog\dog\model_full_768\ppyoloe_plus_fire_full\`。

---

## 方案 B：高分辨率全量训练 (冲刺小目标召回率)
**配置文件**: `configs/custom/ppyoloe_plus_fire_full_hr.yml`
* **分辨率**: 多尺度 `[832, 896, 960, 1024]`
* **Batch Size**: 2 (防显存溢出 OOM)
* **优势**: 能够捕捉更细微的特征，对极远处的火焰、极小的电池目标召回率有潜在提升。
* **代价**: 训练耗时增加 ~80%，推理阶段由于输入图像达 1024x1024，FPS 会大幅下降。

### 1. 启动训练
```bash
cd "D:\work\Marchine Dog\dog\PaddleDetection"

# (可选) 拷贝配置文件
cp "../mycode/configs/ppyoloe_plus_fire_full_hr.yml" "configs/custom/"

# 启动训练
python tools/train.py -c configs/custom/ppyoloe_plus_fire_full_hr.yml --eval
```

### 2. 导出模型
```bash
cd "D:\work\Marchine Dog\dog\PaddleDetection"

# 将最优 epoch 导出为静态图模型
python tools/export_model.py \
  -c configs/custom/ppyoloe_plus_fire_full_hr.yml \
  --output_dir=../model_full_1024 \
  -o use_gpu=False weights=output/ppyoloe_plus_fire_full_hr/best_model.pdparams
```
导出的模型将位于 `D:\work\Marchine Dog\dog\model_full_1024\ppyoloe_plus_fire_full_hr\`。

---

## 常见问题 (FAQ)

### 1. 推理脚本 `predict_2.py` 需要修改吗？
**不需要。**
无论是 `768` 还是 `1024` 的模型，`predict_2.py` 会自动读取导出目录下的 `infer_cfg.yml` 文件。只要你用对应的命令导出了模型，推理代码就会自动提取 `Preprocess -> Resize` 里的分辨率参数并自适应应用，实现代码的 0 修改切换。

### 2. 测试时如何对比这两个模型？
你可以分别修改 `predict_2.py` 底部的 `det_model_path` 指向这两个不同的导出目录：
```python
# 测试 768 版本
det_model_path = r"D:\work\Marchine Dog\dog\model_full_768\ppyoloe_plus_fire_full"

# 测试 1024 版本
# det_model_path = r"D:\work\Marchine Dog\dog\model_full_1024\ppyoloe_plus_fire_full_hr"
```
然后再用线下的 `val_images.txt` 或者打包成 `submission.zip` 提交到线上评测系统看实际得分。

---

## 来源: MODEL_OPTIMIZATION_HISTORY.md

# 模型优化与调参历史档案 (Model Optimization History)
> 本文档由各阶段的调研报告、分析方案与改进计划合并而成。
> **最新状态 (2026-06-16)**：已完成 C1 方案 (PP-YOLOE+ Objects365预训练)，并进行了全面的 FP/FN 分析，准备通过 Hard Negative Mining 方案进行 fine-tune。
>
> ### 💡 给其他 Agent 的交接指南：
> 1. **当前最优模型**：output/ppyoloe_plus_fire_c1/best_model (val F1 ~0.874-0.891)
> 2. **核心瓶颈**：Fire 类别的 FP 绝大部分是高置信度 (score > 0.8) 的真实背景混淆（如反光、灯光），无法通过简单的阈值或 NMS 调整解决。
> 3. **最新数据准备**：已从训练集中提取 42 张包含真实背景误检（Hard Negatives）和漏检（Hard Positives）的图像，生成了带数据增强的 A_train/coco_dataset/annotations/instance_train_finetune.json。
> 4. **下一步行动 (Action Item)**：运行基于 Hard Negative Mining 的 fine-tune：
>    `ash
>    cd PaddleDetection
>    python tools/train.py -c configs/custom/ppyoloe_plus_fire_hn.yml --eval
>    `
>    配置文件已准备好，使用 0.0001 的极小学习率，50 epochs，关闭 Mosaic，热启动 c1 权重。

---

\n## 历史归档: improvement_plan.md\n\n# 模型改进方案报告

> **当前成绩**：官方测试集 F1 = **0.89108**，FPS = 32.62（2026-06-16 12:54）
> **目标**：进一步提升 F1，同时保持 FPS ≥ 20

---

## 一、当前问题诊断（基于 val 集分析）

### 1.1 各类别指标分解

| 类别 | TP | FP | FN | Precision | Recall | F1 |
|------|----|----|-----|-----------|--------|----|
| battery | 5 | 2* | 0 | 0.714 | 1.000 | **0.833** |
| board | 8 | 1 | 1 | 0.889 | 0.889 | **0.889** |
| fire | 135 | 21 | 9 | 0.865 | 0.938 | **0.900** |
| **综合** | — | — | — | — | — | **0.874** |

> [!NOTE]
> \* battery 的 2 个 FP 经人工确认是原数据集**漏标**导致的，模型检测结果完全正确。
> 扣除漏标因素后，battery 实际 P=1.000, R=1.000, F1=**1.000**。

### 1.2 核心瓶颈分析

**主要瓶颈：fire 的 25 个 FP（精确率不足）**

- fire 的 FP 为 25 个，是压制综合 F1 的主因。
- fire 也存在 9 个 FN，说明在 Precision 和 Recall 之间已接近最优平衡点，需要从根本上提升模型质量。

**当前推理参数：**
```python
class_thresholds = {1: 0.50, 2: 0.3, 3: 0.4}   # 置信度阈值
nms_threshold    = {1: 0.4,  2: 0.5, 3: 0.5}    # NMS IoU 阈值
```

> [!WARNING]
> 由于当前 `val_result_2.json` 输出格式**未保存置信度得分**（所有预测 score 显示为 1.0），
> 无法直接在 val 结果上做置信度网格扫参，需要先修改输出格式保存 score。

---

## 二、改进方案（按优先级排序）

---

### 方案 A：保存置信度分布 + 精确扫参（推荐首先执行）

**难度：低 | 风险：低 | 预期收益：+0.005~0.015**

**步骤：**
1. 修改 `predict_2.py` 的结果输出格式，在每条结果中额外保存 `score` 字段。
2. 在 val 集上跑出带 score 的原始预测 json（阈值设为极低，如 0.1）。
3. 写扫参脚本，对 fire 的 `threshold`（0.35~0.55 步长 0.05）和 `nms_threshold`（0.35~0.55 步长 0.05）做网格搜索，找到 F1 最优的参数组合。
4. 更新 `predict_2.py` 中的参数。

**原因：**
目前 fire 有 25 FP 和 9 FN，精确的扫参可以找到比当前 `threshold=0.4, nms=0.5` 更好的平衡点。

---

### 方案 B：修正标注漏标 + 重训 val 基准

**难度：低 | 风险：低 | 预期收益：+0.01（改善评估准确性）**

**步骤：**
1. 将 `frame_01671.jpg` 和 `frame_01674.jpg` 中确认存在但被漏标的 battery 框，补充到训练集和验证集的 COCO JSON 标注中。
2. 重新用修正后的标注评估 val F1，得到更真实的基准数字。
3. （可选）将这两帧带标注加入训练集，对模型进行少量 finetune。

**原因：**
漏标会导致模型在这些帧上的真实 TP 被错误计为 FP，拉低 Precision。官方测试集是否也存在类似漏标未知，但修正训练/验证集始终是好习惯。

---

### 方案 C：Hard Negative Mining（针对 fire 的高置信 FP 帧）

**难度：中 | 风险：低 | 预期收益：+0.01~0.02**

**步骤：**
1. 已知有 19 帧图像出现了 fire 的 FP 检测，提取这些帧。
2. 对其中没有 fire GT 的区域裁剪为负样本 patch，加入训练集的背景类（或增加强增强）。
3. 重新训练 1~3 个 epoch（fine-tune），让模型对这些类型的背景特征更加鲁棒。

**原因：**
fire 的高 FP 往往是模型对特定背景纹理（强光反射、红色/橙色物体）过度敏感导致的，通过 Hard Negative Mining 让模型"见识"过这些干扰背景，可以显著降低误判率。

---

### 方案 D：重训时加入更多 fire 负样本与数据增强

**难度：中高 | 风险：中 | 预期收益：+0.02~0.03**

**步骤：**
1. 收集更多含有类火源干扰但无火灾的图像（如：工厂灯光、反光金属、焊接弧光等），进行背景类标注。
2. 针对 fire 类增加以下数据增强：
   - **随机色调/饱和度扰动**（让模型不依赖颜色偏橙/红来判断火焰）
   - **Mosaic 拼接**（提升多目标密集场景的泛化性）
   - **Random Erasing**（防止模型过拟合局部纹理特征）
3. 基于以上数据集从头训练或在 model_2 基础上 fine-tune。

---

### 方案 E：Board 的 FN 追踪（次要优化）

**难度：低 | 风险：低 | 预期收益：+0.002~0.005**

- board 存在 1 个 FN（漏检）。
- 降低 board 的置信度阈值（从 0.3 降至 0.25）可能把这个目标捞回来，但需警惕引入新的 FP。
- 建议配合方案 A 的扫参一起做，找到 board 的最优阈值。

---

## 三、执行优先级建议

```
第一步 → 方案 A（扫参，1~2小时，零风险，立竿见影）
第二步 → 方案 B（修正漏标，30分钟，改善数据质量）
第三步 → 方案 C（Hard Negative Mining + finetune，半天，显著提升 fire 精确率）
第四步 → 方案 D（更多数据 + 增强 + 重训，1~2天，最终突破瓶颈）
```

---

## 四、预期改进后的成绩估算

| 方案 | 预计 Val F1 | 预计官方 F1 |
|------|------------|------------|
| 当前 | 0.874 | **0.891** |
| +方案A（扫参） | ~0.885 | ~0.900 |
| +方案A+C（扫参+HNM）| ~0.905 | ~0.915 |
| +方案A+C+D（全套）| ~0.920+ | ~0.930+ |

---

## 五、附注：FPS 余量充足

当前 FPS = 32.62，远超评测最低要求（>=20），因此即使增加少量后处理（如稍微收紧 NMS 扫参），FPS 也不会成为瓶颈。

---

## 六、方案 A 执行结果（扫参分析结论）

### 6.1 原始得分分布

对模型在 val 集的原始预测（threshold=0.01，NMS=0.99）做了得分分布统计：

| 类别 | 总预测数 | 低分段 (<0.1) | 中分段 (0.1-0.5) | 高分段 (>=0.5) |
|------|---------|-------------|----------------|----------------|
| battery | 70 | 62 (89%) | 1 | 7 |
| board | 105 | 94 (90%) | 3 | 8 |
| fire | 292 | 111 (38%) | **31** | **150** |

> battery 和 board 大量低分预测（<0.1）说明当前阈值（0.50/0.30）已经精准过滤噪声。
> fire 的中高分预测明显更多，说明火焰检测的分类边界更模糊，模型对非火焰背景置信度偏高。

### 6.2 网格扫参结果

- **扫参空间**：7680 种组合（battery × board × fire 阈值 × NMS 交叉搜索）
- **最优参数**：`threshold={1:0.40, 2:0.30, 3:0.35}`, `nms={1:0.35, 2:0.40, 3:0.35}`
- **最优 val F1 = 0.8701** — **低于当前实际运行结果 0.8741**

> [!IMPORTANT]
> **扫参无法超越现有参数！** 当前 `predict_2.py` 的阈值/NMS 配置已经接近 val 集的理论上限，纯后处理调参的提升空间已经耗尽。

### 6.3 Fire FP 根因分析（最重要结论）

对 25 个 fire FP（threshold>=0.4）进行精细分类：

| FP 类型 | 数量 | 说明 |
|---------|------|------|
| **真实背景混淆** | **25 (100%)** | 模型对非火区域预测高置信度，与任何 GT 火焰框 IoU < 0.2 |
| NMS 漏压重复框 | 0 | NMS 已无问题，无近似重叠误检 |

背景混淆 FP 的置信度分布：

```
低信心 FP (0.40~0.60):  7 个  — 提阈值到 0.6 可消除，代价是部分 TP 损失
中信心 FP (0.60~0.80):  8 个  — 需阈值>0.80，会大量丢失 TP
高信心 FP (>=0.80):    10 个  — 模型置信度极高（最高 0.919），阈值无法区分，必须靠重训解决
```

> [!CAUTION]
> 10 个 FP 置信度 >=0.8，接近甚至超过部分真实 fire TP。
> 单靠提升阈值无法消除这些误检，同时会损伤 Recall。必须通过重训使模型从根本上区分火焰和类火背景。

### 6.4 Fire FN 根因分析

| FN 类型 | 数量 | 说明 | 可修复？ |
|---------|------|------|---------|
| 模型完全不可见（IoU=0）| 3 | 无任何预测框覆盖 GT | 无法靠调参修复，需重训 |
| 低置信度漏检（score 0.33-0.40）| ~6 | 模型"看到"了但信心不足 | 降阈值可捞回，但代价是新增 FP |

---

## 七、最终结论与行动计划

**val F1 上限已达（~0.874），官方 F1（0.891）高于 val，说明测试集的火焰场景对模型更友好。**

要进一步突破，唯一有效路径是重训模型（方案 C/D）：

```
优先级 1  方案 C：Hard Negative Mining
    收集 19 帧高置信 fire FP 图像 → 标注非火区域为负样本
    → fine-tune 2~3 epoch → 预期消除 10~15 个高信心 FP
    → 预计 official F1 提升至 0.91+

优先级 2  补充 fire 漏标或更多 fire 数据
    修复 3 个模型完全不可见的 FN（可能是标注问题或极端遮挡）
    → 预期召回率从 0.938 提升至 0.960+

优先级 3  方案 D：更多数据 + 完整重训（长期路线）
```

## 八、附注：FPS 余量充足

当前 FPS = 32.62，远超评测最低要求（>=20），
即使增加少量后处理，FPS 也不会成为瓶颈。

---

## 九、Fire FP & FN 可视化图像索引（方案 C 数据准备）

> 所有图像已生成至 `fp_images/` 目录。
>
> 图例：
> - **绿色框 GT:fire** — 真实火焰标注
> - **红色框 FP:score** — 误检（False Positive），模型信心高但无对应 GT
> - **橙色框 FN:fire** — 漏检（False Negative），GT 存在但模型未检出
> - **青色框 TP:score** — 正确检出（供参考）

### 9.1 Fire FP 帧（19 帧，共 25 个误检框）

| 序号 | 文件名 | 路径 |
|------|--------|------|
| 1 | frame_01700.jpg | fp_images/fire_fp/frame_01700.jpg |
| 2 | frame_01710.jpg | fp_images/fire_fp/frame_01710.jpg |
| 3 | frame_01716.jpg | fp_images/fire_fp/frame_01716.jpg |
| 4 | frame_01743.jpg | fp_images/fire_fp/frame_01743.jpg |
| 5 | frame_01744.jpg | fp_images/fire_fp/frame_01744.jpg |
| 6 | frame_01768.jpg | fp_images/fire_fp/frame_01768.jpg |
| 7 | frame_01785.jpg | fp_images/fire_fp/frame_01785.jpg |
| 8 | frame_01792.jpg | fp_images/fire_fp/frame_01792.jpg |
| 9 | frame_01816.jpg | fp_images/fire_fp/frame_01816.jpg |
| 10 | frame_01824.jpg | fp_images/fire_fp/frame_01824.jpg |
| 11 | frame_01840.jpg | fp_images/fire_fp/frame_01840.jpg |
| 12 | frame_01841.jpg | fp_images/fire_fp/frame_01841.jpg |
| 13 | frame_01846.jpg | fp_images/fire_fp/frame_01846.jpg |
| 14 | frame_01865.jpg | fp_images/fire_fp/frame_01865.jpg |
| 15 | frame_01925.jpg | fp_images/fire_fp/frame_01925.jpg |
| 16 | frame_01974.jpg | fp_images/fire_fp/frame_01974.jpg |
| 17 | frame_01975.jpg | fp_images/fire_fp/frame_01975.jpg |
| 18 | frame_01994.jpg | fp_images/fire_fp/frame_01994.jpg |
| 19 | frame_01996.jpg | fp_images/fire_fp/frame_01996.jpg |

### 9.2 Fire FN 帧（7 帧，共 9 个漏检框）

| 序号 | 文件名 | 路径 |
|------|--------|------|
| 1 | frame_01674.jpg | fp_images/fire_fn/frame_01674.jpg |
| 2 | frame_01710.jpg | fp_images/fire_fn/frame_01710.jpg |
| 3 | frame_01745.jpg | fp_images/fire_fn/frame_01745.jpg |
| 4 | frame_01841.jpg | fp_images/fire_fn/frame_01841.jpg |
| 5 | frame_01975.jpg | fp_images/fire_fn/frame_01975.jpg |
| 6 | frame_02005.jpg | fp_images/fire_fn/frame_02005.jpg |
| 7 | frame_02022.jpg | fp_images/fire_fn/frame_02022.jpg |

> [!NOTE]
> frame_01710.jpg、frame_01841.jpg、frame_01975.jpg **同时出现在 FP 和 FN 列表中**，
> 说明这些帧中模型在错误位置检测到了火（FP），同时漏掉了真实的火（FN）。
> 这类帧是 Hard Negative Mining 的**重点标注目标**。

### 9.3 Hard Negative Mining 操作建议

1. 将上述 **FP 帧的误检区域**（红色框区域）裁剪为背景负样本，
   加入训练集，标注为"非火"区域（或直接作为 background 类）。
2. 对 **FN 帧**进行人工复查，确认是真实漏标还是极端遮挡：
   - 若是漏标 → 补充标注后加入训练集。
   - 若是真实极端遮挡 → 增加类似场景的数据增强。
3. Fine-tune model_2 约 3~5 个 epoch，使用较小学习率（如 1e-4）。
\n\n---\n\n## 历史归档: fp_analysis_report.md\n\n# 模型误检 (False Positive) 全面分析报告

在对 `model_2` 验证集表现进行分析时，我们定位到了各个类别的 **假阳性 (FP)** 情况。由于整体的精确率（Precision）主要受到 FP 的影响，下面我们针对这几个类别逐一展开可视化分析。

> [!NOTE]
> 图例说明：
> - **绿色/青色框 (GT)** 代表人工的真实标注框。
> - **红色/橙色框 (FP)** 代表模型错误预测的、未能与 GT 匹配的框（即误检）。

---

## 1. Battery 类别：其实是完美表现！
之前评估数据显示 battery 有 2 个 FP，但可视化后确认：**这 2 个所谓的“误检”，实际上是原图真实存在的电池，只是人工漏标了！**

![Battery FP 1](./fp_images/fp_battery_frame_01671.jpg)
![Battery FP 2](./fp_images/fp_battery_frame_01674.jpg)

**结论：** 
模型并没有真正误判，它甚至比人工标注更准确。因此，对于 `battery` 而言，它的实际 Precision 达到了 100%（没有任何实质误检），Recall 也是 100%。

---

## 2. Board 类别：仅 1 个误检
`board` 类在验证集中只有 1 个假阳性，导致它的精确率降为了 0.8889。

### 唯一误检图像：`frame_01983.jpg`
![Board FP](./fp_images/fp_board_frame_01983.jpg)

**分析原因：**
请观察上方的橙色 `FP Board` 框。这通常是因为：
1. **背景纹理干扰**：由于 board（电路板或平板）常常呈矩形且带有特定纹理，一些类似矩形、带有网格或条纹的背景物体容易被误认为 board。
2. **极少量误检**：整个验证集仅出现 1 次，属于极小概率的偶然误差，对整体性能影响不大。

---

## 3. Fire 类别：主要拖累 Precision 的元凶
`fire` 类出现了 **21 个假阳性**，使它的 Precision 降到了 0.8654（尽管 Recall 高达 0.9375）。这里我们提取了置信度最高的 5 个火灾误检样本进行展示：

### 误检图像示例 (Top 5 极高置信度 FP)：
![Fire FP 1](./fp_images/fp_fire_frame_01700.jpg)
![Fire FP 2](./fp_images/fp_fire_frame_01710.jpg)
![Fire FP 3](./fp_images/fp_fire_frame_01716.jpg)
![Fire FP 4](./fp_images/fp_fire_frame_01743.jpg)
![Fire FP 5](./fp_images/fp_fire_frame_01744.jpg)

**分析原因：**
从大量的高分 FP 可以看出，导致 `fire` 精确率较低的原因可能有以下几种：
1. **重叠的重复检测框（NMS 阈值过高）**：如果红色框和绿色 GT 框指向同一个火源，但偏移了一点，这就意味着是非极大值抑制（NMS）没有过滤干净。这是目前最有可能的原因（尤其是密集火苗时）。
2. **类火源干扰物**：强烈的灯光反光、明亮的橙黄色反光物体、甚至模糊的红色/黄色光源都会被模型当成火焰。
3. **模糊和微小特征**：火的形态本身就极不规则。如果在较暗的环境中有微弱的发光点，模型倾向于宁可错杀也不放过（这解释了为什么 fire 的召回率非常高）。

---

### 总结与优化建议
整体 P 值（精确率）被拉低，主要归因于 **Fire 的 21 个 FP**。
* **对于 Battery 和 Board**：目前状态已经极为优秀（几乎 0 误判）。
* **对于 Fire**：建议在 `predict.py` 中将 fire 的 `nms_threshold` 稍微调低（例如从 0.5 降到 0.45 甚至 0.4），并适当提升一点置信度阈值（比如从 0.4 提到 0.45），以此来强力清洗重复框和边缘弱光源的误判，这样 P 值会有立竿见影的提升。
\n\n---\n\n## 历史归档: 训练结果改进调研报告.md\n\n# 火焰检测项目训练结果改进调研报告

**项目**: 基于 PP-YOLOE-s 的火焰/电池/指示牌目标检测
**最终 F1**: 0.9189 | **目标 F1**: 0.85 | **状态**: ✅ 已达标
**报告日期**: 2026-06-13

---

## 一、当前训练结果概述

### 1.1 已执行的优化方案

| 阶段 | F1 | 关键改进 |
|------|-----|----------|
| 初始模型 | 0.80614 | baseline |
| 学习率提升 | 0.82368 | base_lr: 0.0005 → 0.01 |
| 阈值优化 | 0.8455 | battery=0.4, board=0.4, fire=0.5 |
| NMS 后处理 | 0.8701 | predict.py 添加 NMS (threshold=0.55) |
| 扩框优化 | **0.9189** | battery 扩框 1.55x/1.15y, fire 扩框 1.18x/1.18y |

### 1.2 最终成绩

| 数据集 | 综合 F1 | battery F1 | board F1 | fire F1 |
|--------|---------|------------|----------|---------|
| 训练集 | **0.9408** | 0.906 | 0.973 | 0.943 |
| 验证集 | **0.9189** | 0.875 | 0.919 | 0.963 |

**综合 F1 超过目标 0.85，所有类别均达标！**

### 1.3 关键配置参数（最终版）

| 参数 | 当前值 | 原始值 | 说明 |
|------|--------|--------|------|
| 模型 | PP-YOLOE-s | PP-YOLOE-s | CSPResNet backbone, depth_mult=0.33, width_mult=0.50 |
| Epoch | 200 | 200 | CosineDecay 调度 |
| Batch Size | 4 | 4 | 受限于 8GB VRAM |
| 学习率 | 0.0005 | 0.0005 | CosineDecay + LinearWarmup(5 epochs) |
| close_mosaic | 30 | 30 | 最后 30 个 epoch 关闭 Mosaic/Mixup |
| NMS | thresh=0.7, score=0.01 | thresh=0.7, score=0.01 | 模型内 NMS |
| 输入尺寸 | 640/768/896 多尺度 | 640/768/896 | 验证时 keep_ratio=True + PadBatch |
| 数据增强 | Mosaic 0.5, Mixup 0.3, GridMask 0.4, MotionBlur 0.3, GaussianNoise 0.2 | 较弱增强 | 多种增强组合防过拟合 |

### 1.4 推理后处理配置（关键优化）

`predict.py` 实现了以下后处理流水线：
1. **阈值过滤**：class_thresholds = {1: 0.4, 2: 0.4, 3: 0.5}
2. **class-wise NMS**：nms_threshold = 0.55
3. **扩框**：battery (1.55x, 1.15y), fire (1.18x, 1.18y)
4. **clip 到图片边界**
5. **输出**

board 不扩框，只靠 NMS 去重。battery/fire 在 NMS 后扩框，不影响 board 去重。

### 1.5 类别分布

| 类别 | 标注数量 | 占比 | 说明 |
|------|----------|------|------|
| fire | 717 | 73.2% | 主导类别 |
| battery | 151 | 15.4% | 少数类别 |
| board | 112 | 11.4% | 少数类别 |

**训练图片**: 324 张（原始数据，无过采样）

---

## 二、核心问题诊断与解决

### 2.1 ✅ 预训练权重与模型架构匹配（已解决）

**问题**: 之前存在 shape 不匹配警告，因为配置与预训练权重不一致。

**解决方案**: 当前配置已正确设置：
- `depth_mult: 0.33`
- `width_mult: 0.50`
- 预训练权重: `ppyoloe_crn_s_300e_coco.pdparams`

---

### 2.2 ⚠️ class_weight 配置未生效（已知限制）

**问题描述**: PPYOLOEHead 的 `loss_weight` 参数只接受 dict 格式 `{class: 1.0, iou: 2.5, dfl: 0.5}`，不支持 per-class 权重。

**当前状态**: VarifocalLoss 不支持 per-class 权重，类别不平衡通过以下方式缓解：
- 推理后处理中对不同类别使用不同阈值
- battery/fire 使用扩框策略提升召回

---

### 2.3 数据规模（已通过后处理优化缓解）

**当前数据**: 324 张训练图片 + 81 张验证图片

**缓解措施**:
- 强数据增强：Mosaic、Mixup、GridMask、MotionBlur、GaussianNoise
- 推理后处理优化：class-wise NMS + 扩框策略
- 最终 F1 达到 0.9189，超过目标 0.85

---

## 三、优化历程回顾

### 3.1 模型训练优化

#### 阶段 1：基础配置修复
- 修复预训练权重匹配（depth_mult=0.33, width_mult=0.50）
- 配置正确的数据增强参数
- **F1**: 0.80614 → 0.82368

#### 阶段 2：阈值优化
- 使用 `find_best_thresholds.py` 进行网格搜索
- 找到最优类别阈值：battery=0.4, board=0.4, fire=0.5
- **F1**: 0.82368 → 0.8455

#### 阶段 3：NMS 后处理
- 在 `predict.py` 中添加 class-wise NMS
- nms_threshold=0.55
- **F1**: 0.8455 → 0.8701

#### 阶段 4：扩框优化（关键突破）
- battery 扩框：1.55x, 1.15y
- fire 扩框：1.18x, 1.18y
- board 不扩框，只靠 NMS 去重
- **F1**: 0.8701 → **0.9189**

---

### 3.2 数据增强策略

当前配置使用多种增强组合：

| 增强方法 | 参数 | 作用 |
|----------|------|------|
| Mosaic | prob=0.5 | 四图拼接，增加上下文多样性 |
| Mixup | prob=0.3 | 两图混合，增强泛化能力 |
| GridMask | prob=0.4 | 网格遮挡，模拟遮挡场景 |
| MotionBlur | prob=0.3 | 运动模糊，模拟相机运动 |
| GaussianNoise | prob=0.2 | 高斯噪声，模拟传感器噪声 |
| RandomDistort | - | 颜色抖动，增强颜色鲁棒性 |
| RandomExpand | - | 随机扩展，增强尺度多样性 |
| RandomCrop | - | 随机裁剪，增强位置多样性 |
| RandomFlip | - | 水平翻转，增强方向多样性 |

---

### 3.3 推理后处理策略

**当前最优后处理流水线**：

1. **阈值过滤**
   - battery: 0.4
   - board: 0.4
   - fire: 0.5

2. **Class-wise NMS**
   - nms_threshold: 0.55

3. **扩框策略**
   - battery: 1.55x, 1.15y（小目标扩框更激进）
   - fire: 1.18x, 1.18y
   - board: 不扩框

4. **Clip 到图片边界**

**设计思路**：
- battery 是最小目标，需要更激进的扩框来提升召回
- fire 是中等目标，适度扩框
- board 是最大目标，不需要扩框，靠 NMS 去重即可

---

## 四、优化成果总结

### 4.1 已完成的优化

| 优先级 | 方案 | 预期提升 | 实际提升 | 状态 |
|--------|------|----------|----------|------|
| 🔴 P0 | 修复预训练权重匹配 | +0.02~0.05 | +0.017 | ✅ 已完成 |
| 🔴 P0 | 阈值优化 | +0.005~0.01 | +0.022 | ✅ 已完成 |
| 🔴 P0 | NMS 后处理 | +0.01~0.02 | +0.025 | ✅ 已完成 |
| 🔴 P0 | 扩框优化 | +0.02~0.04 | **+0.049** | ✅ 已完成 |

**总提升**: 0.80614 → **0.9189** (+0.113, +14.0%)

### 4.2 关键突破点

1. **扩框策略是最大贡献者**
   - battery 扩框 1.55x/1.15y 提升小目标召回
   - fire 扩框 1.18x/1.18y 提升中等目标召回
   - 单项贡献 +0.049 F1

2. **Class-wise 阈值和 NMS**
   - 不同类别使用不同置信度阈值
   - Class-wise NMS 减少重复检测
   - 贡献 +0.047 F1

3. **数据增强组合**
   - Mosaic + Mixup + GridMask + MotionBlur + GaussianNoise
   - 有效防止过拟合，提升泛化能力

---

## 五、实际效果总结

### 5.1 F1 提升历程

| 阶段 | F1 | 提升 | 关键措施 |
|------|-----|------|----------|
| 初始模型 | 0.80614 | - | baseline |
| 学习率提升 | 0.82368 | +0.018 | base_lr: 0.0005 → 0.01 |
| 阈值优化 | 0.8455 | +0.022 | battery=0.4, board=0.4, fire=0.5 |
| NMS 后处理 | 0.8701 | +0.025 | predict.py 添加 NMS (threshold=0.55) |
| 扩框优化 | **0.9189** | **+0.049** | battery 扩框 1.55x/1.15y, fire 扩框 1.18x/1.18y |

### 5.2 最终成绩

| 数据集 | 综合 F1 | battery F1 | board F1 | fire F1 |
|--------|---------|------------|----------|---------|
| 训练集 | **0.9408** | 0.906 | 0.973 | 0.943 |
| 验证集 | **0.9189** | 0.875 | 0.919 | 0.963 |

**综合 F1 超过目标 0.85 达 8.1%，所有类别均达标！**

### 5.3 优化效果分析

**最大贡献因素**：
1. **扩框优化** (+0.049): 针对不同类别使用不同扩框系数，显著提升小目标召回
2. **NMS 后处理** (+0.025): Class-wise NMS 减少重复检测，提升精确率
3. **阈值优化** (+0.022): 网格搜索找到最优类别阈值

**关键发现**：
- 后处理优化比模型训练优化更有效
- 针对不同类别使用不同策略是关键
- 扩框策略对小目标（battery）效果显著

---

## 六、风险与注意事项

### 6.1 当前方案的局限性

1. **后处理过拟合风险**: 当前最优 F1 依赖于针对验证集调优的后处理参数
   - 扩框系数（1.55x/1.15y 等）是针对当前验证集调出的
   - 如果官方隐藏测试集分布与验证集有偏移，可能表现下降

2. **数据规模限制**: 仅 324 张训练图片，模型泛化能力有限
   - 验证集仅 81 张，评估结果方差较大
   - 更多数据可以提升模型基础能力

3. **VRAM 限制**: 8GB VRAM 限制了 batch_size 和模型大小
   - batch_size=4 影响 BN 统计稳定性
   - 无法使用更大模型（PP-YOLOE-l/x）

### 6.2 泛化风险缓解建议

根据 10.1 节的复盘，当前存在"验证集后处理过拟合"风险：

1. **保持保守策略**: 官方平台提交版应使用保守的后处理参数
2. **减少激进扩框**: 避免过度依赖类别特化扩框
3. **多划分验证**: 使用多次随机重划分验证方案稳定性
4. **关注 battery**: battery 是最脆弱类别，需要重点优化

---

## 七、附录：关键文件清单

### 7.1 配置文件

| 文件 | 用途 | 状态 |
|------|------|------|
| `mycode/configs/ppyoloe_fire.yml` | PP-YOLOE-s 最终训练配置 | ✅ 当前使用 |
| `mycode/configs/ppyoloe_fire_a1.yml` | PP-YOLOE-s A1 调参版 | ✅ 已验证 |
| `mycode/configs/ppyoloe_fire_hn.yml` | Hard Negative 训练配置 | ✅ 已验证 |
| `RT-DETR/configs/rtdetr_r18vd_fire.yml` | RT-DETR-R18 训练配置 | ⏸ 待试验 |

### 7.2 工具脚本

| 文件 | 用途 | 状态 |
|------|------|------|
| `mycode/predict.py` | 推理脚本（含后处理） | ✅ 当前使用 |
| `mycode/calculate_f1.py` | F1 计算 | ✅ 已完成 |
| `mycode/find_best_thresholds.py` | 阈值优化 | ✅ 已完成 |
| `mycode/hard_negative_mining.py` | Hard Negative 挖掘 | ✅ 已完成 |

### 7.3 补丁文件

| 文件 | 用途 |
|------|------|
| `mycode/operators_patch.py` | PaddleDetection 数据增强算子补丁 |
| `mycode/PADDLEDETECTION_PATCHES.md` | 修改说明文档 |

---

## 八、历史优化方案记录

### 8.1 方案 A1：PP-YOLOE-s 调参优化（2026-06-11）

**变更明细**：

| 参数 | 原始 | A1 | 理由 |
|------|------|-----|------|
| epoch | 200 | 350 | 小数据集+强增强需更多迭代 |
| warmup_epochs | 5 | 12 | 配合 lr=0.01 需更长预热 |
| close_mosaic | 30 | 55 | 延长无 Mosaic 精调期 |
| nms_threshold | 0.7 | 0.55 | 降低阈值减少漏检 |
| score_threshold | 0.03 | 0.05 | 提高以减少低置信度 FP |
| GridMask prob | 0.4 | 0.5 | 加强正则化防过拟合 |

配置文件：`mycode/configs/ppyoloe_fire_a1.yml`

### 8.2 方案 B1：RT-DETR-R18（2026-06-11）

新建 `RT-DETR/` 方案目录，包括：
- `configs/rtdetr_r18vd_fire.yml`：完整训练配置（300 epochs, AdamW, lr=0.0001, batch_size=4）
- `README.md`：方案文档（架构对比、配置说明、训练步骤）
- `train.bat`：一键训练脚本

RT-DETR 优势：
- Transformer 端到端检测，无需 NMS 后处理
- HungarianMatcher 天然对 minority 类别（board/battery）友好
- DINOHead + VarifocalLoss 对类别不平衡有抑制
- ResNet18 backbone ~78MB，RTX 4060 8GB 可训练

---

## 九、泛化风险分析与建议

### 9.1 当前方案的泛化风险

当前 F1=0.9189 的高分依赖于针对验证集调优的后处理参数：

| 风险因素 | 具体表现 | 影响程度 |
|----------|----------|----------|
| 扩框系数过拟合 | battery 1.55x/1.15y, fire 1.18x/1.18y 是针对 val 调出的 | 🔴 高 |
| 阈值过拟合 | battery=0.4, board=0.4, fire=0.5 是针对 val 优化的 | 🟡 中 |
| Class-wise NMS | nms_threshold=0.55 是针对 val 调出的 | 🟡 中 |

**核心问题**：如果官方隐藏测试集分布与当前 val 有偏移，这些参数可能失效。

### 9.2 泛化风险缓解建议

#### 短期（立即执行）

1. **使用保守参数提交**
   - 统一阈值 0.4（不区分类别）
   - 减少或取消扩框
   - 保持基础 NMS

2. **多划分验证**
   - 使用 3-fold 或多次随机重划分
   - 记录每次的 battery/board/fire/overall F1
   - 选择"均值更高、方差更小"的方案

#### 中期（下一阶段）

1. **提升模型基础能力**
   - 收集更多训练数据（目标 1000+ 张）
   - 改进 battery 标注质量
   - 做 battery 实例级增强（copy-paste）

2. **减少后处理依赖**
   - 优化模型本身对小目标的检测能力
   - 使用 keep_ratio=True 保持目标形状
   - 尝试更高输入分辨率（768/896）

3. **并行试验 RT-DETR**
   - Transformer 架构对小数据集更友好
   - 端到端检测，无需 NMS 后处理
   - 可能对 battery 类别更友好

### 9.3 结果判断标准

后续方案是否值得保留，按以下标准判断：

1. **battery F1 是否稳定提升**
   - 不只看单次 val，要看多次划分的均值
   - battery 是最脆弱类别，需要重点优化

2. **综合 F1 是否在多次划分下保持一致**
   - 方差要小，不能只靠单次运气
   - 换划分掉分严重的方案不可靠

3. **官方平台分数是否与本地验证趋势一致**
   - 本地 val 很高但 test 掉很多 = 过拟合
   - 本地 val 稳定、test 也稳定 = 可靠方案

4. **是否减少了对阈值和扩框的依赖**
   - 依赖越少，泛化能力越强
   - 模型本身能力越强，后处理越简单

---

## 十、总结与下一步

### 10.1 项目成果

| 指标 | 初始 | 最终 | 提升 |
|------|------|------|------|
| 综合 F1 | 0.80614 | **0.9189** | +14.0% |
| battery F1 | - | 0.875 | - |
| board F1 | - | 0.919 | - |
| fire F1 | - | 0.963 | - |
| 目标达成 | 0/1 | **1/1** | ✅ |

### 10.2 关键经验

1. **后处理优化效果显著**
   - 扩框策略对小目标（battery）效果最好
   - Class-wise 阈值和 NMS 比统一参数更有效
   - 后处理优化比模型训练优化更快速见效

2. **数据增强很重要**
   - Mosaic + Mixup + GridMask + MotionBlur + GaussianNoise 组合有效
   - 强增强可以缓解数据量不足的问题
   - close_mosaic 策略有助于后期精调

3. **针对不同类别使用不同策略**
   - battery（小目标）：低阈值 + 大幅扩框
   - fire（中等目标）：中等阈值 + 适度扩框
   - board（大目标）：标准阈值 + 不扩框

### 10.3 下一步建议

| 优先级 | 方案 | 预期收益 | 实施难度 |
|--------|------|----------|----------|
| 🔴 P0 | 多划分验证 | 减少过拟合风险 | 低 |
| 🔴 P0 | 保守参数提交 | 提升泛化能力 | 低 |
| 🟡 P1 | 收集更多数据 | 提升模型基础能力 | 高 |
| 🟡 P1 | battery 实例级增强 | 提升 battery 召回 | 中 |
| 🟢 P2 | RT-DETR 试验 | 探索新架构 | 中 |

**核心目标**：把当前"高 val 分"转成"高泛化分"，在官方隐藏测试集上也能保持稳定表现。

---

*报告生成时间: 2026-06-13*
*数据来源: 项目日志、配置文件、代码分析、训练结果*
\n\n---\n\n## 历史归档: ppyoloe_plus_objects365_c1_2026-06-15.md\n\n# PP-YOLOE+ Objects365 C1 - 2026-06-15

## Purpose

Hidden test F1 dropped to about `0.84` while visible val F1 was much higher.
The likely cause is weak generalization from a small validation set plus val-specific post-processing.

The C1 direction uses PP-YOLOE+ s with Objects365 pretraining to improve feature transfer while keeping model size far below the `200MB` limit.

## Active Config

- Authoritative config: `mycode/configs/ppyoloe_plus_fire_c1.yml`
- Training copy: `PaddleDetection/configs/custom/ppyoloe_plus_fire_c1.yml`
- Pretrain weights: `ppyoloe_crn_s_obj365_pretrained.pdparams`
- Scale: `depth_mult: 0.33`, `width_mult: 0.50`
- Save dir: `output/ppyoloe_plus_fire_c1`

Both config files are synchronized.

## Geometry Policy

C1 was updated to match the corrected official prediction geometry:

- `TrainReader.BatchRandomResize.keep_ratio: True`
- `TrainReader.batch_transforms` includes `PadBatch: {pad_to_stride: 32}` and `PadGT: {}`
- `EvalReader.Resize target_size: [768, 768], keep_ratio: True`
- `TestReader.Resize target_size: [768, 768], keep_ratio: True`
- Eval/Test include `PadBatch: {pad_to_stride: 32}`
- `eval_size: ~` disables fixed `[640, 640]` anchors inherited from the PP-YOLOE+ reader base.
- `TestReader.inputs_def.image_shape: [3, -1, -1]` avoids fixed static `640x640` export shape.

For a 1080x1920 image, the expected prediction preprocessing is still:

1. resize to `432x768`;
2. pad to `3x448x768`;
3. keep `im_shape=[432,768]` and `scale_factor=[0.4,0.4]`.

## Verification

Reader validation:

```bash
D:/Anaconda/envs/dog/python.exe D:/work/Marchine\ Dog/dog/mycode/scripts/validate_reader_batch_transforms.py
```

Result:

- `ppyoloe_plus_fire_c1.yml TrainReader`: `BatchRandomResize`, `NormalizeImage`, `Permute`, `PadBatch`, `PadGT`
- `EvalReader`: `Resize`, `NormalizeImage`, `Permute` + `PadBatch`
- `TestReader`: `Resize`, `NormalizeImage`, `Permute` + `PadBatch`
- validation passed.

One-epoch smoke test:

```bash
cd D:/work/Marchine\ Dog/dog/PaddleDetection
D:/Anaconda/envs/dog/python.exe tools/train.py -c configs/custom/ppyoloe_plus_fire_c1.yml --eval -o epoch=1 snapshot_epoch=1
```

Result:

- Objects365 pretrained weights downloaded and loaded.
- Classification head weights were skipped because the pretrained head has 365 classes and this task has 3 classes. This is expected.
- Train loop, evaluation loop, keep-ratio padding, and checkpoint save all completed.
- Checkpoints were saved under `output/ppyoloe_plus_fire_c1`.
- One-epoch eval AP was not meaningful for model selection, but confirmed the pipeline works.

## Full Training Command

Run this in normal or high-performance power mode:

```bash
cd D:/work/Marchine\ Dog/dog/PaddleDetection
D:/Anaconda/envs/dog/python.exe tools/train.py -c configs/custom/ppyoloe_plus_fire_c1.yml --eval
```

After training, export the best model:

```bash
cd D:/work/Marchine\ Dog/dog/PaddleDetection
D:/Anaconda/envs/dog/python.exe tools/export_model.py -c configs/custom/ppyoloe_plus_fire_c1.yml --output_dir=../../model_ppyoloe_plus_c1 -o weights=output/ppyoloe_plus_fire_c1/best_model.pdparams
```

Then copy or point the submission `predict.py` to the exported model directory and retune class thresholds on raw scored predictions.

## Retuning Rule

Do not reuse the old PP-YOLOE-s thresholds blindly.
After exporting C1:

1. generate raw scored val predictions;
2. search battery threshold and class-wise NMS;
3. compare raw-score evaluation and no-score submission replay;
4. prefer stable parameters over tiny val-only gains.
\n\n---\n\n## 历史归档: predict_keep_ratio_retune_2026-06-15.md\n\n# predict.py keep_ratio retune - 2026-06-15

## Scope

- Official submission script: `D:/work/Marchine Dog/predict.py`.
- Model config: `D:/work/Marchine Dog/model/infer_cfg.yml`.
- The exported model declares `Resize target_size: [768, 768]`, `keep_ratio: true`, and `PadStride stride: 32`.

## Root Cause

Before this change, `predict.py` ignored `keep_ratio=True` and always stretched images to `768x768`.
For a 1080x1920 image, the correct exported-model preprocessing is:

1. resize with ratio kept: `1080x1920 -> 432x768`;
2. `PadStride(32)`: tensor becomes `3x448x768`;
3. `im_shape` must stay `[432, 768]`;
4. `scale_factor` must be `[0.4, 0.4]`.

The batch `create_inputs()` path also previously replaced `im_shape` with the padded batch shape.
That diverges from `PaddleDetection/deploy/python/infer.py`; only `image` should be batch-padded.

## Code Changes

`D:/work/Marchine Dog/predict.py` was updated as follows:

- `Resize.__call__()` now honors `keep_ratio=True`.
- `create_inputs()` now preserves preprocessing `im_shape` and `scale_factor`.
- `image` tensors are still padded to the largest batch shape for batched inference.
- Battery threshold was retuned after the geometry fix.

## Selected Submission Parameters

```python
class_thresholds = {1: 0.50, 2: 0.30, 3: 0.40}
nms_threshold = {
    1: 0.40,
    2: 0.50,
    3: 0.50,
}
bbox_scales = {
    1: (1.00, 1.00),
    2: (1.00, 1.00),
    3: (1.00, 1.00),
}
```

Battery threshold `0.50` removes one low-score FP (`score=0.4529`) while the lowest retained battery TP is still about `0.6926`.

Raw val search also found `fire NMS=0.60` with overall F1 `0.8935`, but the gain over `fire NMS=0.50` was only `+0.0003` and it added fire FP. It was not applied to reduce validation-set overfitting risk.

## Verification

Preprocess regression check:

```bash
D:/Anaconda/envs/dog/python.exe -m py_compile "D:/work/Marchine Dog/predict.py"
```

Official-style validation run:

```bash
D:/Anaconda/envs/dog/python.exe "D:/work/Marchine Dog/predict.py" "D:/work/Marchine Dog/val.txt" "D:/work/Marchine Dog/tmp_keep_ratio_battery_tuned.json"
D:/Anaconda/envs/dog/python.exe "D:/work/Marchine Dog/dog/mycode/calculate_f1.py" "D:/work/Marchine Dog/tmp_keep_ratio_battery_tuned.json" "D:/work/Marchine Dog/dog/A_train/coco/annotations/instance_val.json"
```

Result:

| Class | TP | FP | FN | Precision | Recall | F1 |
|---|---:|---:|---:|---:|---:|---:|
| battery | 7 | 1 | 1 | 0.8750 | 0.8750 | 0.8750 |
| board | 16 | 1 | 3 | 0.9412 | 0.8421 | 0.8889 |
| fire | 174 | 12 | 20 | 0.9355 | 0.8969 | 0.9158 |

- Overall F1: `0.8932`.
- Runtime: `2.5629s` for 81 images.
- FPS: about `31.6`, above the official `20 FPS` constraint.

## Follow-up Notes

- If retuning again, evaluate both raw scored predictions and no-score submission replay. The local `calculate_f1.py` treats missing scores as `1.0`, so output order can affect matching.
- Do not retune only against the raw scored evaluator and then copy the raw-best parameters blindly.
- Avoid changing `fire NMS` unless the gain is meaningful on a less val-specific split or on cross-validation.
\n\n---\n\n## 历史归档: fire_postprocess_tuning_guide.md\n\n# Fire Postprocess Tuning Guide

This note documents the script for tuning fire post-processing without blindly overfitting the validation set.

## Goal

Use `mycode/scripts/tune_fire_postprocess.py` to search:

- fire score threshold
- fire NMS IoU threshold
- fire small-box minimum area

The script reports both:

- `best_raw`: the highest validation F1 in the search space
- `recommended_stable`: a conservative choice near the best score

Prefer `recommended_stable` first. It penalizes overly high thresholds, overly strict NMS, large min-area filtering, and clear fire recall drops.

## Generate Raw Scored Predictions

The official submission JSON usually does not contain `score`, so it cannot be used for threshold tuning directly.

Use the real submission script to export raw scored predictions:

```bash
python mycode/scripts/tune_fire_postprocess.py --predict-py "D:/work/Marchine Dog/predict.py" --infer-txt val.txt --gt A_train/coco/annotations/instance_val.json --raw-out raw_preds_with_score.json --out fire_postprocess_tuning.json
```

By default, the model directory is resolved as `D:/work/Marchine Dog/model`. Override it when needed:

```bash
python mycode/scripts/tune_fire_postprocess.py --predict-py "D:/work/Marchine Dog/predict.py" --model-dir "D:/work/Marchine Dog/model" --infer-txt val.txt --gt A_train/coco/annotations/instance_val.json
```

## Reuse Existing Raw Predictions

If `raw_preds_with_score.json` already exists:

```bash
python mycode/scripts/tune_fire_postprocess.py --pred-json raw_preds_with_score.json --gt A_train/coco/annotations/instance_val.json --out fire_postprocess_tuning.json
```

## Practical Search Range

The defaults are intentionally small and conservative:

- `fire_thresholds`: `0.30,0.35,0.40,0.45,0.50,0.55`
- `fire_nms_values`: `0.45,0.50,0.55,0.60`
- `fire_min_areas`: `0,50,80,120,160,200`

For quick score improvement when fire FP is high, inspect candidates around:

- fire threshold: `0.40` to `0.50`
- fire NMS: `0.45` to `0.55`
- fire min area: `50` to `120`

Avoid jumping directly to extreme values unless the hidden test result confirms the trend.

## Verification

Run:

```bash
python mycode/scripts/test_tune_fire_postprocess.py
python -m py_compile mycode/scripts/tune_fire_postprocess.py mycode/scripts/test_tune_fire_postprocess.py
```
\n\n---\n

---

## 来源: ppyoloe_plus_objects365_c1_2026-06-15.md

# PP-YOLOE+ Objects365 C1 - 2026-06-15

## Purpose

Hidden test F1 dropped to about `0.84` while visible val F1 was much higher.
The likely cause is weak generalization from a small validation set plus val-specific post-processing.

The C1 direction uses PP-YOLOE+ s with Objects365 pretraining to improve feature transfer while keeping model size far below the `200MB` limit.

## Active Config

- Authoritative config: `mycode/configs/ppyoloe_plus_fire_c1.yml`
- Training copy: `PaddleDetection/configs/custom/ppyoloe_plus_fire_c1.yml`
- Pretrain weights: `ppyoloe_crn_s_obj365_pretrained.pdparams`
- Scale: `depth_mult: 0.33`, `width_mult: 0.50`
- Save dir: `output/ppyoloe_plus_fire_c1`

Both config files are synchronized.

## Geometry Policy

C1 was updated to match the corrected official prediction geometry:

- `TrainReader.BatchRandomResize.keep_ratio: True`
- `TrainReader.batch_transforms` includes `PadBatch: {pad_to_stride: 32}` and `PadGT: {}`
- `EvalReader.Resize target_size: [768, 768], keep_ratio: True`
- `TestReader.Resize target_size: [768, 768], keep_ratio: True`
- Eval/Test include `PadBatch: {pad_to_stride: 32}`
- `eval_size: ~` disables fixed `[640, 640]` anchors inherited from the PP-YOLOE+ reader base.
- `TestReader.inputs_def.image_shape: [3, -1, -1]` avoids fixed static `640x640` export shape.

For a 1080x1920 image, the expected prediction preprocessing is still:

1. resize to `432x768`;
2. pad to `3x448x768`;
3. keep `im_shape=[432,768]` and `scale_factor=[0.4,0.4]`.

## Verification

Reader validation:

```bash
D:/Anaconda/envs/dog/python.exe D:/work/Marchine\ Dog/dog/mycode/scripts/validate_reader_batch_transforms.py
```

Result:

- `ppyoloe_plus_fire_c1.yml TrainReader`: `BatchRandomResize`, `NormalizeImage`, `Permute`, `PadBatch`, `PadGT`
- `EvalReader`: `Resize`, `NormalizeImage`, `Permute` + `PadBatch`
- `TestReader`: `Resize`, `NormalizeImage`, `Permute` + `PadBatch`
- validation passed.

One-epoch smoke test:

```bash
cd D:/work/Marchine\ Dog/dog/PaddleDetection
D:/Anaconda/envs/dog/python.exe tools/train.py -c configs/custom/ppyoloe_plus_fire_c1.yml --eval -o epoch=1 snapshot_epoch=1
```

Result:

- Objects365 pretrained weights downloaded and loaded.
- Classification head weights were skipped because the pretrained head has 365 classes and this task has 3 classes. This is expected.
- Train loop, evaluation loop, keep-ratio padding, and checkpoint save all completed.
- Checkpoints were saved under `output/ppyoloe_plus_fire_c1`.
- One-epoch eval AP was not meaningful for model selection, but confirmed the pipeline works.

## Full Training Command

Run this in normal or high-performance power mode:

```bash
cd D:/work/Marchine\ Dog/dog/PaddleDetection
D:/Anaconda/envs/dog/python.exe tools/train.py -c configs/custom/ppyoloe_plus_fire_c1.yml --eval
```

After training, export the best model:

```bash
cd D:/work/Marchine\ Dog/dog/PaddleDetection
D:/Anaconda/envs/dog/python.exe tools/export_model.py -c configs/custom/ppyoloe_plus_fire_c1.yml --output_dir=../../model_ppyoloe_plus_c1 -o weights=output/ppyoloe_plus_fire_c1/best_model.pdparams
```

Then copy or point the submission `predict.py` to the exported model directory and retune class thresholds on raw scored predictions.

## Retuning Rule

Do not reuse the old PP-YOLOE-s thresholds blindly.
After exporting C1:

1. generate raw scored val predictions;
2. search battery threshold and class-wise NMS;
3. compare raw-score evaluation and no-score submission replay;
4. prefer stable parameters over tiny val-only gains.

---

## 来源: predict_keep_ratio_retune_2026-06-15.md

# predict.py keep_ratio retune - 2026-06-15

## Scope

- Official submission script: `D:/work/Marchine Dog/predict.py`.
- Model config: `D:/work/Marchine Dog/model/infer_cfg.yml`.
- The exported model declares `Resize target_size: [768, 768]`, `keep_ratio: true`, and `PadStride stride: 32`.

## Root Cause

Before this change, `predict.py` ignored `keep_ratio=True` and always stretched images to `768x768`.
For a 1080x1920 image, the correct exported-model preprocessing is:

1. resize with ratio kept: `1080x1920 -> 432x768`;
2. `PadStride(32)`: tensor becomes `3x448x768`;
3. `im_shape` must stay `[432, 768]`;
4. `scale_factor` must be `[0.4, 0.4]`.

The batch `create_inputs()` path also previously replaced `im_shape` with the padded batch shape.
That diverges from `PaddleDetection/deploy/python/infer.py`; only `image` should be batch-padded.

## Code Changes

`D:/work/Marchine Dog/predict.py` was updated as follows:

- `Resize.__call__()` now honors `keep_ratio=True`.
- `create_inputs()` now preserves preprocessing `im_shape` and `scale_factor`.
- `image` tensors are still padded to the largest batch shape for batched inference.
- Battery threshold was retuned after the geometry fix.

## Selected Submission Parameters

```python
class_thresholds = {1: 0.50, 2: 0.30, 3: 0.40}
nms_threshold = {
    1: 0.40,
    2: 0.50,
    3: 0.50,
}
bbox_scales = {
    1: (1.00, 1.00),
    2: (1.00, 1.00),
    3: (1.00, 1.00),
}
```

Battery threshold `0.50` removes one low-score FP (`score=0.4529`) while the lowest retained battery TP is still about `0.6926`.

Raw val search also found `fire NMS=0.60` with overall F1 `0.8935`, but the gain over `fire NMS=0.50` was only `+0.0003` and it added fire FP. It was not applied to reduce validation-set overfitting risk.

## Verification

Preprocess regression check:

```bash
D:/Anaconda/envs/dog/python.exe -m py_compile "D:/work/Marchine Dog/predict.py"
```

Official-style validation run:

```bash
D:/Anaconda/envs/dog/python.exe "D:/work/Marchine Dog/predict.py" "D:/work/Marchine Dog/val.txt" "D:/work/Marchine Dog/tmp_keep_ratio_battery_tuned.json"
D:/Anaconda/envs/dog/python.exe "D:/work/Marchine Dog/dog/mycode/calculate_f1.py" "D:/work/Marchine Dog/tmp_keep_ratio_battery_tuned.json" "D:/work/Marchine Dog/dog/A_train/coco/annotations/instance_val.json"
```

Result:

| Class | TP | FP | FN | Precision | Recall | F1 |
|---|---:|---:|---:|---:|---:|---:|
| battery | 7 | 1 | 1 | 0.8750 | 0.8750 | 0.8750 |
| board | 16 | 1 | 3 | 0.9412 | 0.8421 | 0.8889 |
| fire | 174 | 12 | 20 | 0.9355 | 0.8969 | 0.9158 |

- Overall F1: `0.8932`.
- Runtime: `2.5629s` for 81 images.
- FPS: about `31.6`, above the official `20 FPS` constraint.

## Follow-up Notes

- If retuning again, evaluate both raw scored predictions and no-score submission replay. The local `calculate_f1.py` treats missing scores as `1.0`, so output order can affect matching.
- Do not retune only against the raw scored evaluator and then copy the raw-best parameters blindly.
- Avoid changing `fire NMS` unless the gain is meaningful on a less val-specific split or on cross-validation.

---

## 来源: README.md

# 火焰检测项目

基于 PaddleDetection 的 PP-YOLOE-s 进行火焰/电池/指示牌目标检测。

## 环境要求

- Windows 10/11
- NVIDIA GPU（支持 CUDA）
- CUDA 12.x
- cuDNN 8.x+
- Anaconda 或 Miniconda

## 安装环境

### 1. 创建 Conda 环境

```bash
conda create -n dog python=3.10 -y
conda activate dog
```

### 2. 安装 PaddlePaddle GPU 版本

```bash
# 安装 PaddlePaddle GPU 版本（CUDA 12.3）
pip install paddlepaddle-gpu==3.0.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu123/
```

验证安装：
```bash
python -c "import paddle; print('CUDA:', paddle.is_compiled_with_cuda()); paddle.utils.run_check()"
```

应输出：
```
CUDA: True
PaddlePaddle works well on 1 GPU.
```

### 3. 克隆并安装 PaddleDetection

```bash
cd D:\work\Marchine Dog\dog
git clone https://github.com/PaddlePaddle/PaddleDetection.git
cd PaddleDetection
pip install -r requirements.txt
python setup.py install
```

### 4. 安装其他依赖

```bash
pip install opencv-python<=4.6.0 pyyaml pillow packaging>=21.0
pip install numpy<2.0 visualdl>=2.2.0 pycocotools==2.0.8 imgaug>=0.4.0
```

### 5. 验证环境

```bash
python -c "from ppdet.data.transform.operators import MotionBlur, GaussianNoise; print('Enhancement operators loaded successfully!')"
```

## 项目结构

```
dog/
├── A_train/                          # 训练数据
│   ├── Image/                        # 原始图片（405张）
│   ├── label/                        # LabelMe 格式标注
│   └── coco/                         # COCO 格式数据
│       ├── train/                    # 训练集图片（324张）
│       ├── val/                      # 验证集图片（81张）
│       └── annotations/              # COCO 标注文件
│           ├── instance_train.json
│           └── instance_val.json
├── model/                            # 导出的推理模型
│   ├── model.pdmodel
│   ├── model.pdiparams
│   └── infer_cfg.yml
├── mycode/                           # 自定义代码
│   ├── predict.py                    # 推理脚本（含 NMS + 扩框）
│   ├── calculate_f1.py               # F1 评估脚本
│   ├── find_best_thresholds.py       # 阈值优化脚本
│   ├── find_best_thresholds_v2.py    # 阈值优化脚本 v2
│   ├── test_fps.py                   # FPS 测试脚本
│   ├── hard_negative_mining.py       # Hard Negative 挖掘脚本
│   ├── analyze_fp.py                 # FP 分析脚本
│   ├── analyze_fp_features.py        # FP 特征分析脚本
│   ├── analyze_fp_stats.py           # FP 统计分析脚本
│   ├── add_hard_negative.py          # 添加 Hard Negative 脚本
│   ├── check_conf.py                 # 检查 conf 分布脚本
│   ├── copy_paste_augmentation.py    # Copy-Paste 数据增强脚本
│   ├── offline_augmentation.py       # 离线数据增强脚本
│   └── configs/                      # 训练配置备份
│       ├── ppyoloe_fire.yml          # PP-YOLOE-s 训练配置
│       ├── ppyoloe_fire_a1.yml       # PP-YOLOE-s A1 调参版
│       ├── ppyoloe_fire_hn.yml       # Hard Negative 训练配置
│       └── ppyoloe_crn.yml           # PP-YOLOE 模型架构配置
├── PaddleDetection/                  # PaddleDetection 框架
├── RT-DETR/                          # RT-DETR 方案
│   ├── configs/rtdetr_r18vd_fire.yml #   RT-DETR-R18 训练配置
│   ├── README.md                     #   方案文档
│   └── train.bat                     #   训练脚本
├── ppyolo/                           # 预训练权重
├── README.md                         # 本文件
├── AGENTS.md                         # Agent 配置
├── Agent.md                          # 配置变更历史
├── 训练结果改进调研报告.md            # 优化方案调研
└── requirements.txt                  # 依赖列表
```

## 类别说明

| 类别 ID | 类别名称 | 描述 |
|---------|----------|------|
| 1 | battery | 电池 |
| 2 | board | 指示牌 |
| 3 | fire | 火焰 |

## 训练验证指令

训练配置使用相对数据路径，默认从 `PaddleDetection/` 目录启动。移动项目到其他电脑时，只要保持 `dog/PaddleDetection` 和 `dog/A_train` 的相对位置不变，无需改配置中的数据路径。

### 1. 训练模型

```bash
cd PaddleDetection
python tools/train.py -c configs/custom/ppyoloe_fire.yml --eval
```

### 2. 导出模型

```bash
cd PaddleDetection
python tools/export_model.py -c configs/custom/ppyoloe_fire.yml --output_dir=../../model -o weights=output/best_model.pdparams
```

### 3. 推理预测

```bash
python predict.py <data_txt> <result_json>
```

示例：
```bash
python predict.py val.txt val_result.json
```

### 4. 评估 F1 值

```bash
python mycode/calculate_f1.py val_result.json dog/A_train/coco/annotations/instance_val.json
```

### 5. 测试 FPS

```bash
python mycode/test_fps.py
```

## 训练配置

### PP-YOLOE-s 当前配置（`mycode/configs/ppyoloe_fire.yml`）

| 参数 | 值 | 说明 |
|------|-----|------|
| 模型 | PP-YOLOE-s | 预训练权重：COCO，depth_mult=0.33, width_mult=0.50 |
| Epoch | 200 | 小数据集训练轮数 |
| Batch Size | 4 | 受限于 RTX 4060 8GB VRAM |
| 学习率 | 0.01 | CosineDecay + LinearWarmup(5 epochs) |
| 优化器 | Momentum | 动量=0.9 |
| 权重衰减 | 0.0005 | L2 正则化 |
| close_mosaic | 30 | 最后 30 轮关闭 Mosaic/Mixup |
| NMS threshold | 0.55 | 降低以减少漏检 |
| Score threshold | 0.01 | 导出模型使用低阈值 |

### 推理后处理配置（`predict.py`）

| 参数 | 值 | 说明 |
|------|-----|------|
| class_thresholds | {1: 0.4, 2: 0.4, 3: 0.5} | 最优阈值（F1=0.9189） |
| nms_threshold | 0.55 | NMS IoU 阈值 |
| bbox_scales | {1: (1.55, 1.15), 2: (1.0, 1.0), 3: (1.18, 1.18)} | 扩框比例 |

处理顺序：阈值过滤 → class-wise NMS → battery/fire 扩框 → clip 到图片边界 → 输出

### 数据增强配置

| 增强操作 | 概率 | 说明 |
|----------|------|------|
| Mosaic | 0.5 | 四图拼接 + Mixup(0.3) |
| GridMask | 0.4 | 网格遮挡正则化 |
| RandomDistort | - | 颜色抖动（亮度/对比度/饱和度/色调） |
| MotionBlur | 0.3 | 运动模糊 |
| GaussianNoise | 0.2 | 高斯噪声 |
| RandomExpand | - | 随机扩展 |
| RandomCrop | - | 随机裁剪 |
| RandomFlip | - | 随机水平翻转 |
| BatchRandomResize | - | 多尺度训练（640/768/896） |

### 类别分布

| 类别 | 实例数 | 占比 |
|------|--------|------|
| battery | 151 | 15.4% |
| board | 112 | 11.4% |
| fire | 717 | 73.2% |

> **注意**：VarifocalLoss 不支持 per-class 权重，`class_weight` 配置会被静默忽略。
> 类别不平衡通过推理后处理（NMS + 扩框）解决。

## 性能指标

### 最终成绩（2026-06-12）

| 数据集 | 综合 F1 | battery F1 | board F1 | fire F1 |
|--------|---------|------------|----------|---------|
| 训练集 | **0.9408** | 0.906 | 0.973 | 0.943 |
| 验证集 | **0.9189** | 0.875 | 0.919 | 0.963 |

**综合 F1 超过目标 0.85，所有类别均达标！**

### 优化历程

| 阶段 | F1 | 关键改进 |
|------|-----|----------|
| 初始模型 | 0.80614 | baseline |
| 学习率提升 | 0.82368 | base_lr: 0.0005 → 0.01 |
| 阈值优化 | 0.8455 | battery=0.4, board=0.4, fire=0.5 |
| NMS 后处理 | 0.8701 | predict.py 添加 NMS (threshold=0.55) |
| 扩框优化 | **0.9189** | battery 扩框 1.55x/1.15y, fire 扩框 1.18x/1.18y |

### 泛化验证

训练集/验证集 F1 差距仅 2.2%，无过拟合风险。

## 常见问题

### 1. CUDA 不可用

检查 CUDA 环境变量：
```bash
echo %CUDA_HOME%
echo %PATH%
```

如未设置，执行：
```bash
set CUDA_HOME=C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.3
set PATH=%CUDA_HOME%\bin;%PATH%
```

### 2. PaddleDetection 导入错误

确保已安装 PaddleDetection：
```bash
cd PaddleDetection
python setup.py install
```

### 3. 增强算子未找到

确保使用的是 dog 环境的 Python：
```bash
where.exe python
# 应显示 D:\Anaconda\envs\dog\python.exe
```

### 4. 内存不足

减小 batch_size：
```yaml
# 修改 ppyoloe_fire.yml
TrainReader:
  batch_size: 2  # 默认为 4
```

### 5. 配置不生效

`class_weight` 列表格式在 PPYOLOEHead 中不被识别（仅支持 `{class, iou, dfl}` dict）。
VarifocalLoss 不支持 per-class 权重，需通过推理后处理解决类别不平衡。
详见 `Agent.md` 和 `AGENTS.md`。

### 6. 训练时报错 `ValueError: all input arrays must have the same shape`

这是 `DataLoader` 组 batch 时的典型配置问题，不是图片文件本身损坏。

常见根因：
- batch 内不同图片的目标框数量不同
- `TrainReader.batch_transforms` 里缺少 `PadGT: {}`
- `default_collate_fn` 在拼接 `gt_bbox / gt_class / is_crowd` 时直接 `np.stack` 失败

修复方式：
```yaml
TrainReader:
  batch_transforms:
    - PadBatch: {pad_to_stride: 32}
    - PadGT: {}
```

快速校验：
```bash
python mycode/scripts/validate_reader_batch_transforms.py
```

如果你在自定义 `RandomResize`、`keep_ratio`、`PadBatch` 或 reader 流程后再次遇到这个报错，先检查 `PadGT` 是否还在。

### 7. 验证时报错 `custom_pan.py` 中 `paddle.concat` 尺寸不一致

典型报错类似：
- `input[0] shape = [1, 192, 28, 48]`
- `input[1] shape = [1, 256, 27, 48]`

这通常不是模型权重损坏，而是 `EvalReader/TestReader` 的输入尺寸没有对齐到稳定的 32 倍数。

常见根因：
- 使用了 `Resize(..., keep_ratio=True)`
- 但没有额外做 `PadBatch`
- `Resize(keep_ratio=True)` 只等比缩放，不会自动补边
- 结果某些图片在进入 PP-YOLOE 的 FPN/PAN 后，上采样分支和旁路分支会出现 `27/28` 这种错一格

修复方式：
```yaml
EvalReader:
  sample_transforms:
    - Decode: {}
    - Resize: {target_size: [768, 768], keep_ratio: True, interp: 2}
    - NormalizeImage: {mean: [0.485, 0.456, 0.406], std: [0.229, 0.224, 0.225], is_scale: True}
    - Permute: {}
  batch_transforms:
    - PadBatch: {pad_to_stride: 32}

TestReader:
  sample_transforms:
    - Decode: {}
    - Resize: {target_size: [768, 768], keep_ratio: True, interp: 2}
    - NormalizeImage: {mean: [0.485, 0.456, 0.406], std: [0.229, 0.224, 0.225], is_scale: True}
    - Permute: {}
  batch_transforms:
    - PadBatch: {pad_to_stride: 32}
```

快速校验：
```bash
python mycode/scripts/validate_reader_batch_transforms.py
```

如果你打算保留 `keep_ratio=True` 的验证/测试流程，就把 `PadBatch` 当成配套项，不要单独开启。

## 许可证

本项目基于 PaddleDetection 开发，遵循 Apache 2.0 许可证。

---

## 来源: 训练结果改进调研报告.md

# 火焰检测项目训练结果改进调研报告

**项目**: 基于 PP-YOLOE-s 的火焰/电池/指示牌目标检测
**最终 F1**: 0.9189 | **目标 F1**: 0.85 | **状态**: ✅ 已达标
**报告日期**: 2026-06-13

---

## 一、当前训练结果概述

### 1.1 已执行的优化方案

| 阶段 | F1 | 关键改进 |
|------|-----|----------|
| 初始模型 | 0.80614 | baseline |
| 学习率提升 | 0.82368 | base_lr: 0.0005 → 0.01 |
| 阈值优化 | 0.8455 | battery=0.4, board=0.4, fire=0.5 |
| NMS 后处理 | 0.8701 | predict.py 添加 NMS (threshold=0.55) |
| 扩框优化 | **0.9189** | battery 扩框 1.55x/1.15y, fire 扩框 1.18x/1.18y |

### 1.2 最终成绩

| 数据集 | 综合 F1 | battery F1 | board F1 | fire F1 |
|--------|---------|------------|----------|---------|
| 训练集 | **0.9408** | 0.906 | 0.973 | 0.943 |
| 验证集 | **0.9189** | 0.875 | 0.919 | 0.963 |

**综合 F1 超过目标 0.85，所有类别均达标！**

### 1.3 关键配置参数（最终版）

| 参数 | 当前值 | 原始值 | 说明 |
|------|--------|--------|------|
| 模型 | PP-YOLOE-s | PP-YOLOE-s | CSPResNet backbone, depth_mult=0.33, width_mult=0.50 |
| Epoch | 200 | 200 | CosineDecay 调度 |
| Batch Size | 4 | 4 | 受限于 8GB VRAM |
| 学习率 | 0.0005 | 0.0005 | CosineDecay + LinearWarmup(5 epochs) |
| close_mosaic | 30 | 30 | 最后 30 个 epoch 关闭 Mosaic/Mixup |
| NMS | thresh=0.7, score=0.01 | thresh=0.7, score=0.01 | 模型内 NMS |
| 输入尺寸 | 640/768/896 多尺度 | 640/768/896 | 验证时 keep_ratio=True + PadBatch |
| 数据增强 | Mosaic 0.5, Mixup 0.3, GridMask 0.4, MotionBlur 0.3, GaussianNoise 0.2 | 较弱增强 | 多种增强组合防过拟合 |

### 1.4 推理后处理配置（关键优化）

`predict.py` 实现了以下后处理流水线：
1. **阈值过滤**：class_thresholds = {1: 0.4, 2: 0.4, 3: 0.5}
2. **class-wise NMS**：nms_threshold = 0.55
3. **扩框**：battery (1.55x, 1.15y), fire (1.18x, 1.18y)
4. **clip 到图片边界**
5. **输出**

board 不扩框，只靠 NMS 去重。battery/fire 在 NMS 后扩框，不影响 board 去重。

### 1.5 类别分布

| 类别 | 标注数量 | 占比 | 说明 |
|------|----------|------|------|
| fire | 717 | 73.2% | 主导类别 |
| battery | 151 | 15.4% | 少数类别 |
| board | 112 | 11.4% | 少数类别 |

**训练图片**: 324 张（原始数据，无过采样）

---

## 二、核心问题诊断与解决

### 2.1 ✅ 预训练权重与模型架构匹配（已解决）

**问题**: 之前存在 shape 不匹配警告，因为配置与预训练权重不一致。

**解决方案**: 当前配置已正确设置：
- `depth_mult: 0.33`
- `width_mult: 0.50`
- 预训练权重: `ppyoloe_crn_s_300e_coco.pdparams`

---

### 2.2 ⚠️ class_weight 配置未生效（已知限制）

**问题描述**: PPYOLOEHead 的 `loss_weight` 参数只接受 dict 格式 `{class: 1.0, iou: 2.5, dfl: 0.5}`，不支持 per-class 权重。

**当前状态**: VarifocalLoss 不支持 per-class 权重，类别不平衡通过以下方式缓解：
- 推理后处理中对不同类别使用不同阈值
- battery/fire 使用扩框策略提升召回

---

### 2.3 数据规模（已通过后处理优化缓解）

**当前数据**: 324 张训练图片 + 81 张验证图片

**缓解措施**:
- 强数据增强：Mosaic、Mixup、GridMask、MotionBlur、GaussianNoise
- 推理后处理优化：class-wise NMS + 扩框策略
- 最终 F1 达到 0.9189，超过目标 0.85

---

## 三、优化历程回顾

### 3.1 模型训练优化

#### 阶段 1：基础配置修复
- 修复预训练权重匹配（depth_mult=0.33, width_mult=0.50）
- 配置正确的数据增强参数
- **F1**: 0.80614 → 0.82368

#### 阶段 2：阈值优化
- 使用 `find_best_thresholds.py` 进行网格搜索
- 找到最优类别阈值：battery=0.4, board=0.4, fire=0.5
- **F1**: 0.82368 → 0.8455

#### 阶段 3：NMS 后处理
- 在 `predict.py` 中添加 class-wise NMS
- nms_threshold=0.55
- **F1**: 0.8455 → 0.8701

#### 阶段 4：扩框优化（关键突破）
- battery 扩框：1.55x, 1.15y
- fire 扩框：1.18x, 1.18y
- board 不扩框，只靠 NMS 去重
- **F1**: 0.8701 → **0.9189**

---

### 3.2 数据增强策略

当前配置使用多种增强组合：

| 增强方法 | 参数 | 作用 |
|----------|------|------|
| Mosaic | prob=0.5 | 四图拼接，增加上下文多样性 |
| Mixup | prob=0.3 | 两图混合，增强泛化能力 |
| GridMask | prob=0.4 | 网格遮挡，模拟遮挡场景 |
| MotionBlur | prob=0.3 | 运动模糊，模拟相机运动 |
| GaussianNoise | prob=0.2 | 高斯噪声，模拟传感器噪声 |
| RandomDistort | - | 颜色抖动，增强颜色鲁棒性 |
| RandomExpand | - | 随机扩展，增强尺度多样性 |
| RandomCrop | - | 随机裁剪，增强位置多样性 |
| RandomFlip | - | 水平翻转，增强方向多样性 |

---

### 3.3 推理后处理策略

**当前最优后处理流水线**：

1. **阈值过滤**
   - battery: 0.4
   - board: 0.4
   - fire: 0.5

2. **Class-wise NMS**
   - nms_threshold: 0.55

3. **扩框策略**
   - battery: 1.55x, 1.15y（小目标扩框更激进）
   - fire: 1.18x, 1.18y
   - board: 不扩框

4. **Clip 到图片边界**

**设计思路**：
- battery 是最小目标，需要更激进的扩框来提升召回
- fire 是中等目标，适度扩框
- board 是最大目标，不需要扩框，靠 NMS 去重即可

---

## 四、优化成果总结

### 4.1 已完成的优化

| 优先级 | 方案 | 预期提升 | 实际提升 | 状态 |
|--------|------|----------|----------|------|
| 🔴 P0 | 修复预训练权重匹配 | +0.02~0.05 | +0.017 | ✅ 已完成 |
| 🔴 P0 | 阈值优化 | +0.005~0.01 | +0.022 | ✅ 已完成 |
| 🔴 P0 | NMS 后处理 | +0.01~0.02 | +0.025 | ✅ 已完成 |
| 🔴 P0 | 扩框优化 | +0.02~0.04 | **+0.049** | ✅ 已完成 |

**总提升**: 0.80614 → **0.9189** (+0.113, +14.0%)

### 4.2 关键突破点

1. **扩框策略是最大贡献者**
   - battery 扩框 1.55x/1.15y 提升小目标召回
   - fire 扩框 1.18x/1.18y 提升中等目标召回
   - 单项贡献 +0.049 F1

2. **Class-wise 阈值和 NMS**
   - 不同类别使用不同置信度阈值
   - Class-wise NMS 减少重复检测
   - 贡献 +0.047 F1

3. **数据增强组合**
   - Mosaic + Mixup + GridMask + MotionBlur + GaussianNoise
   - 有效防止过拟合，提升泛化能力

---

## 五、实际效果总结

### 5.1 F1 提升历程

| 阶段 | F1 | 提升 | 关键措施 |
|------|-----|------|----------|
| 初始模型 | 0.80614 | - | baseline |
| 学习率提升 | 0.82368 | +0.018 | base_lr: 0.0005 → 0.01 |
| 阈值优化 | 0.8455 | +0.022 | battery=0.4, board=0.4, fire=0.5 |
| NMS 后处理 | 0.8701 | +0.025 | predict.py 添加 NMS (threshold=0.55) |
| 扩框优化 | **0.9189** | **+0.049** | battery 扩框 1.55x/1.15y, fire 扩框 1.18x/1.18y |

### 5.2 最终成绩

| 数据集 | 综合 F1 | battery F1 | board F1 | fire F1 |
|--------|---------|------------|----------|---------|
| 训练集 | **0.9408** | 0.906 | 0.973 | 0.943 |
| 验证集 | **0.9189** | 0.875 | 0.919 | 0.963 |

**综合 F1 超过目标 0.85 达 8.1%，所有类别均达标！**

### 5.3 优化效果分析

**最大贡献因素**：
1. **扩框优化** (+0.049): 针对不同类别使用不同扩框系数，显著提升小目标召回
2. **NMS 后处理** (+0.025): Class-wise NMS 减少重复检测，提升精确率
3. **阈值优化** (+0.022): 网格搜索找到最优类别阈值

**关键发现**：
- 后处理优化比模型训练优化更有效
- 针对不同类别使用不同策略是关键
- 扩框策略对小目标（battery）效果显著

---

## 六、风险与注意事项

### 6.1 当前方案的局限性

1. **后处理过拟合风险**: 当前最优 F1 依赖于针对验证集调优的后处理参数
   - 扩框系数（1.55x/1.15y 等）是针对当前验证集调出的
   - 如果官方隐藏测试集分布与验证集有偏移，可能表现下降

2. **数据规模限制**: 仅 324 张训练图片，模型泛化能力有限
   - 验证集仅 81 张，评估结果方差较大
   - 更多数据可以提升模型基础能力

3. **VRAM 限制**: 8GB VRAM 限制了 batch_size 和模型大小
   - batch_size=4 影响 BN 统计稳定性
   - 无法使用更大模型（PP-YOLOE-l/x）

### 6.2 泛化风险缓解建议

根据 10.1 节的复盘，当前存在"验证集后处理过拟合"风险：

1. **保持保守策略**: 官方平台提交版应使用保守的后处理参数
2. **减少激进扩框**: 避免过度依赖类别特化扩框
3. **多划分验证**: 使用多次随机重划分验证方案稳定性
4. **关注 battery**: battery 是最脆弱类别，需要重点优化

---

## 七、附录：关键文件清单

### 7.1 配置文件

| 文件 | 用途 | 状态 |
|------|------|------|
| `mycode/configs/ppyoloe_fire.yml` | PP-YOLOE-s 最终训练配置 | ✅ 当前使用 |
| `mycode/configs/ppyoloe_fire_a1.yml` | PP-YOLOE-s A1 调参版 | ✅ 已验证 |
| `mycode/configs/ppyoloe_fire_hn.yml` | Hard Negative 训练配置 | ✅ 已验证 |
| `RT-DETR/configs/rtdetr_r18vd_fire.yml` | RT-DETR-R18 训练配置 | ⏸ 待试验 |

### 7.2 工具脚本

| 文件 | 用途 | 状态 |
|------|------|------|
| `mycode/predict.py` | 推理脚本（含后处理） | ✅ 当前使用 |
| `mycode/calculate_f1.py` | F1 计算 | ✅ 已完成 |
| `mycode/find_best_thresholds.py` | 阈值优化 | ✅ 已完成 |
| `mycode/hard_negative_mining.py` | Hard Negative 挖掘 | ✅ 已完成 |

### 7.3 补丁文件

| 文件 | 用途 |
|------|------|
| `mycode/operators_patch.py` | PaddleDetection 数据增强算子补丁 |
| `mycode/PADDLEDETECTION_PATCHES.md` | 修改说明文档 |

---

## 八、历史优化方案记录

### 8.1 方案 A1：PP-YOLOE-s 调参优化（2026-06-11）

**变更明细**：

| 参数 | 原始 | A1 | 理由 |
|------|------|-----|------|
| epoch | 200 | 350 | 小数据集+强增强需更多迭代 |
| warmup_epochs | 5 | 12 | 配合 lr=0.01 需更长预热 |
| close_mosaic | 30 | 55 | 延长无 Mosaic 精调期 |
| nms_threshold | 0.7 | 0.55 | 降低阈值减少漏检 |
| score_threshold | 0.03 | 0.05 | 提高以减少低置信度 FP |
| GridMask prob | 0.4 | 0.5 | 加强正则化防过拟合 |

配置文件：`mycode/configs/ppyoloe_fire_a1.yml`

### 8.2 方案 B1：RT-DETR-R18（2026-06-11）

新建 `RT-DETR/` 方案目录，包括：
- `configs/rtdetr_r18vd_fire.yml`：完整训练配置（300 epochs, AdamW, lr=0.0001, batch_size=4）
- `README.md`：方案文档（架构对比、配置说明、训练步骤）
- `train.bat`：一键训练脚本

RT-DETR 优势：
- Transformer 端到端检测，无需 NMS 后处理
- HungarianMatcher 天然对 minority 类别（board/battery）友好
- DINOHead + VarifocalLoss 对类别不平衡有抑制
- ResNet18 backbone ~78MB，RTX 4060 8GB 可训练

---

## 九、泛化风险分析与建议

### 9.1 当前方案的泛化风险

当前 F1=0.9189 的高分依赖于针对验证集调优的后处理参数：

| 风险因素 | 具体表现 | 影响程度 |
|----------|----------|----------|
| 扩框系数过拟合 | battery 1.55x/1.15y, fire 1.18x/1.18y 是针对 val 调出的 | 🔴 高 |
| 阈值过拟合 | battery=0.4, board=0.4, fire=0.5 是针对 val 优化的 | 🟡 中 |
| Class-wise NMS | nms_threshold=0.55 是针对 val 调出的 | 🟡 中 |

**核心问题**：如果官方隐藏测试集分布与当前 val 有偏移，这些参数可能失效。

### 9.2 泛化风险缓解建议

#### 短期（立即执行）

1. **使用保守参数提交**
   - 统一阈值 0.4（不区分类别）
   - 减少或取消扩框
   - 保持基础 NMS

2. **多划分验证**
   - 使用 3-fold 或多次随机重划分
   - 记录每次的 battery/board/fire/overall F1
   - 选择"均值更高、方差更小"的方案

#### 中期（下一阶段）

1. **提升模型基础能力**
   - 收集更多训练数据（目标 1000+ 张）
   - 改进 battery 标注质量
   - 做 battery 实例级增强（copy-paste）

2. **减少后处理依赖**
   - 优化模型本身对小目标的检测能力
   - 使用 keep_ratio=True 保持目标形状
   - 尝试更高输入分辨率（768/896）

3. **并行试验 RT-DETR**
   - Transformer 架构对小数据集更友好
   - 端到端检测，无需 NMS 后处理
   - 可能对 battery 类别更友好

### 9.3 结果判断标准

后续方案是否值得保留，按以下标准判断：

1. **battery F1 是否稳定提升**
   - 不只看单次 val，要看多次划分的均值
   - battery 是最脆弱类别，需要重点优化

2. **综合 F1 是否在多次划分下保持一致**
   - 方差要小，不能只靠单次运气
   - 换划分掉分严重的方案不可靠

3. **官方平台分数是否与本地验证趋势一致**
   - 本地 val 很高但 test 掉很多 = 过拟合
   - 本地 val 稳定、test 也稳定 = 可靠方案

4. **是否减少了对阈值和扩框的依赖**
   - 依赖越少，泛化能力越强
   - 模型本身能力越强，后处理越简单

---

## 十、总结与下一步

### 10.1 项目成果

| 指标 | 初始 | 最终 | 提升 |
|------|------|------|------|
| 综合 F1 | 0.80614 | **0.9189** | +14.0% |
| battery F1 | - | 0.875 | - |
| board F1 | - | 0.919 | - |
| fire F1 | - | 0.963 | - |
| 目标达成 | 0/1 | **1/1** | ✅ |

### 10.2 关键经验

1. **后处理优化效果显著**
   - 扩框策略对小目标（battery）效果最好
   - Class-wise 阈值和 NMS 比统一参数更有效
   - 后处理优化比模型训练优化更快速见效

2. **数据增强很重要**
   - Mosaic + Mixup + GridMask + MotionBlur + GaussianNoise 组合有效
   - 强增强可以缓解数据量不足的问题
   - close_mosaic 策略有助于后期精调

3. **针对不同类别使用不同策略**
   - battery（小目标）：低阈值 + 大幅扩框
   - fire（中等目标）：中等阈值 + 适度扩框
   - board（大目标）：标准阈值 + 不扩框

### 10.3 下一步建议

| 优先级 | 方案 | 预期收益 | 实施难度 |
|--------|------|----------|----------|
| 🔴 P0 | 多划分验证 | 减少过拟合风险 | 低 |
| 🔴 P0 | 保守参数提交 | 提升泛化能力 | 低 |
| 🟡 P1 | 收集更多数据 | 提升模型基础能力 | 高 |
| 🟡 P1 | battery 实例级增强 | 提升 battery 召回 | 中 |
| 🟢 P2 | RT-DETR 试验 | 探索新架构 | 中 |

**核心目标**：把当前"高 val 分"转成"高泛化分"，在官方隐藏测试集上也能保持稳定表现。

---

*报告生成时间: 2026-06-13*
*数据来源: 项目日志、配置文件、代码分析、训练结果*
