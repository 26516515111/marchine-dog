# 火焰检测项目训练结果改进调研报告

**项目**: 基于 PP-YOLOE-s 的火焰/电池/指示牌目标检测
**最终 F1**: 0.9189 | **目标 F1**: 0.85 | **状态**: ✅ 已达标
**报告日期**: 2026-06-13

---

## 一、当前训练结果概述

### 1.1 已执行的优化方案

| 阶段 | F1 | 关键改进 |
|------|-----|----------|
| 初始模型 | 0.80614 | baseline |
| 学习率提升 | 0.82368 | base_lr: 0.0005 → 0.01 |
| 阈值优化 | 0.8455 | battery=0.4, board=0.4, fire=0.5 |
| NMS 后处理 | 0.8701 | predict.py 添加 NMS (threshold=0.55) |
| 扩框优化 | **0.9189** | battery 扩框 1.55x/1.15y, fire 扩框 1.18x/1.18y |

### 1.2 最终成绩

| 数据集 | 综合 F1 | battery F1 | board F1 | fire F1 |
|--------|---------|------------|----------|---------|
| 训练集 | **0.9408** | 0.906 | 0.973 | 0.943 |
| 验证集 | **0.9189** | 0.875 | 0.919 | 0.963 |

**综合 F1 超过目标 0.85，所有类别均达标！**

### 1.3 关键配置参数（最终版）

| 参数 | 当前值 | 原始值 | 说明 |
|------|--------|--------|------|
| 模型 | PP-YOLOE-s | PP-YOLOE-s | CSPResNet backbone, depth_mult=0.33, width_mult=0.50 |
| Epoch | 200 | 200 | CosineDecay 调度 |
| Batch Size | 4 | 4 | 受限于 8GB VRAM |
| 学习率 | 0.0005 | 0.0005 | CosineDecay + LinearWarmup(5 epochs) |
| close_mosaic | 30 | 30 | 最后 30 个 epoch 关闭 Mosaic/Mixup |
| NMS | thresh=0.7, score=0.01 | thresh=0.7, score=0.01 | 模型内 NMS |
| 输入尺寸 | 640/768/896 多尺度 | 640/768/896 | 验证时 keep_ratio=True + PadBatch |
| 数据增强 | Mosaic 0.5, Mixup 0.3, GridMask 0.4, MotionBlur 0.3, GaussianNoise 0.2 | 较弱增强 | 多种增强组合防过拟合 |

### 1.4 推理后处理配置（关键优化）

`predict.py` 实现了以下后处理流水线：
1. **阈值过滤**：class_thresholds = {1: 0.4, 2: 0.4, 3: 0.5}
2. **class-wise NMS**：nms_threshold = 0.55
3. **扩框**：battery (1.55x, 1.15y), fire (1.18x, 1.18y)
4. **clip 到图片边界**
5. **输出**

board 不扩框，只靠 NMS 去重。battery/fire 在 NMS 后扩框，不影响 board 去重。

### 1.5 类别分布

| 类别 | 标注数量 | 占比 | 说明 |
|------|----------|------|------|
| fire | 717 | 73.2% | 主导类别 |
| battery | 151 | 15.4% | 少数类别 |
| board | 112 | 11.4% | 少数类别 |

**训练图片**: 324 张（原始数据，无过采样）

---

## 二、核心问题诊断与解决

### 2.1 ✅ 预训练权重与模型架构匹配（已解决）

**问题**: 之前存在 shape 不匹配警告，因为配置与预训练权重不一致。

**解决方案**: 当前配置已正确设置：
- `depth_mult: 0.33`
- `width_mult: 0.50`
- 预训练权重: `ppyoloe_crn_s_300e_coco.pdparams`

---

### 2.2 ⚠️ class_weight 配置未生效（已知限制）

**问题描述**: PPYOLOEHead 的 `loss_weight` 参数只接受 dict 格式 `{class: 1.0, iou: 2.5, dfl: 0.5}`，不支持 per-class 权重。

**当前状态**: VarifocalLoss 不支持 per-class 权重，类别不平衡通过以下方式缓解：
- 推理后处理中对不同类别使用不同阈值
- battery/fire 使用扩框策略提升召回

---

### 2.3 数据规模（已通过后处理优化缓解）

**当前数据**: 324 张训练图片 + 81 张验证图片

**缓解措施**:
- 强数据增强：Mosaic、Mixup、GridMask、MotionBlur、GaussianNoise
- 推理后处理优化：class-wise NMS + 扩框策略
- 最终 F1 达到 0.9189，超过目标 0.85

---

## 三、优化历程回顾

### 3.1 模型训练优化

#### 阶段 1：基础配置修复
- 修复预训练权重匹配（depth_mult=0.33, width_mult=0.50）
- 配置正确的数据增强参数
- **F1**: 0.80614 → 0.82368

#### 阶段 2：阈值优化
- 使用 `find_best_thresholds.py` 进行网格搜索
- 找到最优类别阈值：battery=0.4, board=0.4, fire=0.5
- **F1**: 0.82368 → 0.8455

#### 阶段 3：NMS 后处理
- 在 `predict.py` 中添加 class-wise NMS
- nms_threshold=0.55
- **F1**: 0.8455 → 0.8701

#### 阶段 4：扩框优化（关键突破）
- battery 扩框：1.55x, 1.15y
- fire 扩框：1.18x, 1.18y
- board 不扩框，只靠 NMS 去重
- **F1**: 0.8701 → **0.9189**

---

### 3.2 数据增强策略

当前配置使用多种增强组合：

| 增强方法 | 参数 | 作用 |
|----------|------|------|
| Mosaic | prob=0.5 | 四图拼接，增加上下文多样性 |
| Mixup | prob=0.3 | 两图混合，增强泛化能力 |
| GridMask | prob=0.4 | 网格遮挡，模拟遮挡场景 |
| MotionBlur | prob=0.3 | 运动模糊，模拟相机运动 |
| GaussianNoise | prob=0.2 | 高斯噪声，模拟传感器噪声 |
| RandomDistort | - | 颜色抖动，增强颜色鲁棒性 |
| RandomExpand | - | 随机扩展，增强尺度多样性 |
| RandomCrop | - | 随机裁剪，增强位置多样性 |
| RandomFlip | - | 水平翻转，增强方向多样性 |

---

### 3.3 推理后处理策略

**当前最优后处理流水线**：

1. **阈值过滤**
   - battery: 0.4
   - board: 0.4
   - fire: 0.5

2. **Class-wise NMS**
   - nms_threshold: 0.55

3. **扩框策略**
   - battery: 1.55x, 1.15y（小目标扩框更激进）
   - fire: 1.18x, 1.18y
   - board: 不扩框

4. **Clip 到图片边界**

**设计思路**：
- battery 是最小目标，需要更激进的扩框来提升召回
- fire 是中等目标，适度扩框
- board 是最大目标，不需要扩框，靠 NMS 去重即可

---

## 四、优化成果总结

### 4.1 已完成的优化

| 优先级 | 方案 | 预期提升 | 实际提升 | 状态 |
|--------|------|----------|----------|------|
| 🔴 P0 | 修复预训练权重匹配 | +0.02~0.05 | +0.017 | ✅ 已完成 |
| 🔴 P0 | 阈值优化 | +0.005~0.01 | +0.022 | ✅ 已完成 |
| 🔴 P0 | NMS 后处理 | +0.01~0.02 | +0.025 | ✅ 已完成 |
| 🔴 P0 | 扩框优化 | +0.02~0.04 | **+0.049** | ✅ 已完成 |

**总提升**: 0.80614 → **0.9189** (+0.113, +14.0%)

### 4.2 关键突破点

1. **扩框策略是最大贡献者**
   - battery 扩框 1.55x/1.15y 提升小目标召回
   - fire 扩框 1.18x/1.18y 提升中等目标召回
   - 单项贡献 +0.049 F1

2. **Class-wise 阈值和 NMS**
   - 不同类别使用不同置信度阈值
   - Class-wise NMS 减少重复检测
   - 贡献 +0.047 F1

3. **数据增强组合**
   - Mosaic + Mixup + GridMask + MotionBlur + GaussianNoise
   - 有效防止过拟合，提升泛化能力

---

## 五、实际效果总结

### 5.1 F1 提升历程

| 阶段 | F1 | 提升 | 关键措施 |
|------|-----|------|----------|
| 初始模型 | 0.80614 | - | baseline |
| 学习率提升 | 0.82368 | +0.018 | base_lr: 0.0005 → 0.01 |
| 阈值优化 | 0.8455 | +0.022 | battery=0.4, board=0.4, fire=0.5 |
| NMS 后处理 | 0.8701 | +0.025 | predict.py 添加 NMS (threshold=0.55) |
| 扩框优化 | **0.9189** | **+0.049** | battery 扩框 1.55x/1.15y, fire 扩框 1.18x/1.18y |

### 5.2 最终成绩

| 数据集 | 综合 F1 | battery F1 | board F1 | fire F1 |
|--------|---------|------------|----------|---------|
| 训练集 | **0.9408** | 0.906 | 0.973 | 0.943 |
| 验证集 | **0.9189** | 0.875 | 0.919 | 0.963 |

**综合 F1 超过目标 0.85 达 8.1%，所有类别均达标！**

### 5.3 优化效果分析

**最大贡献因素**：
1. **扩框优化** (+0.049): 针对不同类别使用不同扩框系数，显著提升小目标召回
2. **NMS 后处理** (+0.025): Class-wise NMS 减少重复检测，提升精确率
3. **阈值优化** (+0.022): 网格搜索找到最优类别阈值

**关键发现**：
- 后处理优化比模型训练优化更有效
- 针对不同类别使用不同策略是关键
- 扩框策略对小目标（battery）效果显著

---

## 六、风险与注意事项

### 6.1 当前方案的局限性

1. **后处理过拟合风险**: 当前最优 F1 依赖于针对验证集调优的后处理参数
   - 扩框系数（1.55x/1.15y 等）是针对当前验证集调出的
   - 如果官方隐藏测试集分布与验证集有偏移，可能表现下降

2. **数据规模限制**: 仅 324 张训练图片，模型泛化能力有限
   - 验证集仅 81 张，评估结果方差较大
   - 更多数据可以提升模型基础能力

3. **VRAM 限制**: 8GB VRAM 限制了 batch_size 和模型大小
   - batch_size=4 影响 BN 统计稳定性
   - 无法使用更大模型（PP-YOLOE-l/x）

### 6.2 泛化风险缓解建议

根据 10.1 节的复盘，当前存在"验证集后处理过拟合"风险：

1. **保持保守策略**: 官方平台提交版应使用保守的后处理参数
2. **减少激进扩框**: 避免过度依赖类别特化扩框
3. **多划分验证**: 使用多次随机重划分验证方案稳定性
4. **关注 battery**: battery 是最脆弱类别，需要重点优化

---

## 七、附录：关键文件清单

### 7.1 配置文件

| 文件 | 用途 | 状态 |
|------|------|------|
| `mycode/configs/ppyoloe_fire.yml` | PP-YOLOE-s 最终训练配置 | ✅ 当前使用 |
| `mycode/configs/ppyoloe_fire_a1.yml` | PP-YOLOE-s A1 调参版 | ✅ 已验证 |
| `mycode/configs/ppyoloe_fire_hn.yml` | Hard Negative 训练配置 | ✅ 已验证 |
| `RT-DETR/configs/rtdetr_r18vd_fire.yml` | RT-DETR-R18 训练配置 | ⏸ 待试验 |

### 7.2 工具脚本

| 文件 | 用途 | 状态 |
|------|------|------|
| `mycode/predict.py` | 推理脚本（含后处理） | ✅ 当前使用 |
| `mycode/calculate_f1.py` | F1 计算 | ✅ 已完成 |
| `mycode/find_best_thresholds.py` | 阈值优化 | ✅ 已完成 |
| `mycode/hard_negative_mining.py` | Hard Negative 挖掘 | ✅ 已完成 |

### 7.3 补丁文件

| 文件 | 用途 |
|------|------|
| `mycode/operators_patch.py` | PaddleDetection 数据增强算子补丁 |
| `mycode/PADDLEDETECTION_PATCHES.md` | 修改说明文档 |

---

## 八、历史优化方案记录

### 8.1 方案 A1：PP-YOLOE-s 调参优化（2026-06-11）

**变更明细**：

| 参数 | 原始 | A1 | 理由 |
|------|------|-----|------|
| epoch | 200 | 350 | 小数据集+强增强需更多迭代 |
| warmup_epochs | 5 | 12 | 配合 lr=0.01 需更长预热 |
| close_mosaic | 30 | 55 | 延长无 Mosaic 精调期 |
| nms_threshold | 0.7 | 0.55 | 降低阈值减少漏检 |
| score_threshold | 0.03 | 0.05 | 提高以减少低置信度 FP |
| GridMask prob | 0.4 | 0.5 | 加强正则化防过拟合 |

配置文件：`mycode/configs/ppyoloe_fire_a1.yml`

### 8.2 方案 B1：RT-DETR-R18（2026-06-11）

新建 `RT-DETR/` 方案目录，包括：
- `configs/rtdetr_r18vd_fire.yml`：完整训练配置（300 epochs, AdamW, lr=0.0001, batch_size=4）
- `README.md`：方案文档（架构对比、配置说明、训练步骤）
- `train.bat`：一键训练脚本

RT-DETR 优势：
- Transformer 端到端检测，无需 NMS 后处理
- HungarianMatcher 天然对 minority 类别（board/battery）友好
- DINOHead + VarifocalLoss 对类别不平衡有抑制
- ResNet18 backbone ~78MB，RTX 4060 8GB 可训练

---

## 九、泛化风险分析与建议

### 9.1 当前方案的泛化风险

当前 F1=0.9189 的高分依赖于针对验证集调优的后处理参数：

| 风险因素 | 具体表现 | 影响程度 |
|----------|----------|----------|
| 扩框系数过拟合 | battery 1.55x/1.15y, fire 1.18x/1.18y 是针对 val 调出的 | 🔴 高 |
| 阈值过拟合 | battery=0.4, board=0.4, fire=0.5 是针对 val 优化的 | 🟡 中 |
| Class-wise NMS | nms_threshold=0.55 是针对 val 调出的 | 🟡 中 |

**核心问题**：如果官方隐藏测试集分布与当前 val 有偏移，这些参数可能失效。

### 9.2 泛化风险缓解建议

#### 短期（立即执行）

1. **使用保守参数提交**
   - 统一阈值 0.4（不区分类别）
   - 减少或取消扩框
   - 保持基础 NMS

2. **多划分验证**
   - 使用 3-fold 或多次随机重划分
   - 记录每次的 battery/board/fire/overall F1
   - 选择"均值更高、方差更小"的方案

#### 中期（下一阶段）

1. **提升模型基础能力**
   - 收集更多训练数据（目标 1000+ 张）
   - 改进 battery 标注质量
   - 做 battery 实例级增强（copy-paste）

2. **减少后处理依赖**
   - 优化模型本身对小目标的检测能力
   - 使用 keep_ratio=True 保持目标形状
   - 尝试更高输入分辨率（768/896）

3. **并行试验 RT-DETR**
   - Transformer 架构对小数据集更友好
   - 端到端检测，无需 NMS 后处理
   - 可能对 battery 类别更友好

### 9.3 结果判断标准

后续方案是否值得保留，按以下标准判断：

1. **battery F1 是否稳定提升**
   - 不只看单次 val，要看多次划分的均值
   - battery 是最脆弱类别，需要重点优化

2. **综合 F1 是否在多次划分下保持一致**
   - 方差要小，不能只靠单次运气
   - 换划分掉分严重的方案不可靠

3. **官方平台分数是否与本地验证趋势一致**
   - 本地 val 很高但 test 掉很多 = 过拟合
   - 本地 val 稳定、test 也稳定 = 可靠方案

4. **是否减少了对阈值和扩框的依赖**
   - 依赖越少，泛化能力越强
   - 模型本身能力越强，后处理越简单

---

## 十、总结与下一步

### 10.1 项目成果

| 指标 | 初始 | 最终 | 提升 |
|------|------|------|------|
| 综合 F1 | 0.80614 | **0.9189** | +14.0% |
| battery F1 | - | 0.875 | - |
| board F1 | - | 0.919 | - |
| fire F1 | - | 0.963 | - |
| 目标达成 | 0/1 | **1/1** | ✅ |

### 10.2 关键经验

1. **后处理优化效果显著**
   - 扩框策略对小目标（battery）效果最好
   - Class-wise 阈值和 NMS 比统一参数更有效
   - 后处理优化比模型训练优化更快速见效

2. **数据增强很重要**
   - Mosaic + Mixup + GridMask + MotionBlur + GaussianNoise 组合有效
   - 强增强可以缓解数据量不足的问题
   - close_mosaic 策略有助于后期精调

3. **针对不同类别使用不同策略**
   - battery（小目标）：低阈值 + 大幅扩框
   - fire（中等目标）：中等阈值 + 适度扩框
   - board（大目标）：标准阈值 + 不扩框

### 10.3 下一步建议

| 优先级 | 方案 | 预期收益 | 实施难度 |
|--------|------|----------|----------|
| 🔴 P0 | 多划分验证 | 减少过拟合风险 | 低 |
| 🔴 P0 | 保守参数提交 | 提升泛化能力 | 低 |
| 🟡 P1 | 收集更多数据 | 提升模型基础能力 | 高 |
| 🟡 P1 | battery 实例级增强 | 提升 battery 召回 | 中 |
| 🟢 P2 | RT-DETR 试验 | 探索新架构 | 中 |

**核心目标**：把当前"高 val 分"转成"高泛化分"，在官方隐藏测试集上也能保持稳定表现。

---

*报告生成时间: 2026-06-13*
*数据来源: 项目日志、配置文件、代码分析、训练结果*
