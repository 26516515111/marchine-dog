# 火焰检测项目训练结果改进调研报告

**项目**: 基于 PP-YOLOE-s 的火焰/电池/指示牌目标检测
**当前 F1**: 0.80614 | **目标 F1**: 0.85 | **差距**: 0.04386
**报告日期**: 2026-06-11

---

## 一、当前训练结果概述

### 1.1 已执行的优化方案

| 方案 | 具体措施 | 状态 |
|------|----------|------|
| 学习率调整 | 0.0005 → 0.01（提高 20 倍） | ✅ 已执行 |
| 数据增强增强 | Mosaic 0.3→0.5, Mixup 0.2→0.3, 新增 GridMask 0.4 | ✅ 已执行 |
| close_mosaic | 20 → 30（更长时间干净训练） | ✅ 已执行 |
| 数据过采样 | battery 1份, board 2份（324→584张） | ✅ 已执行 |
| Hard Negative Mining | 324张图片中发现105个FP，提取3个crop | ✅ 已执行 |
| Copy-Paste 增强 | GrabCut 分割 + 泊松融合 | ✅ 已执行 |
| 离线增强 | 旋转90° + 水平翻转 | ✅ 已执行 |
| A1 PP-YOLOE-s 调参 | epoch 200→350, warmup 5→12, close_mosaic 30→55, NMS 0.7→0.55, score 0.03→0.05, GridMask 0.4→0.5 | ✅ 已落地 (2026-06-11) |
| B1 RT-DETR-R18 | 新增 RT-DETR/ 方案目录，完整配置 + 文档 + 训练脚本 | ✅ 已落地 (2026-06-11) |

### 1.2 关键配置参数

| 参数 | 当前值 (A1) | 原始值 | 说明 |
|------|------------|--------|------|
| 模型 | PP-YOLOE-s | PP-YOLOE-s | CSPResNet backbone, depth_mult=0.33, width_mult=0.50 |
| Epoch | 350 | 200 | A1 调参增加迭代 |
| Batch Size | 4 | 4 | 受限于 8GB VRAM |
| 学习率 | 0.01 | 0.01 | CosineDecay + LinearWarmup(12 epochs) |
| close_mosaic | 55 | 30 | A1 延长无 Mosaic 精调期 |
| NMS | thresh=0.55, score=0.05 | thresh=0.7, score=0.03 | A1 降低 NMS 减少漏检，提高 score 减少 FP |
| 输入尺寸 | 640/768/896 多尺度 | 640/768/896 | 验证时固定 640x640 |

### 1.3 类别分布

| 类别 | 原始样本占比 | 过采样后 | 标注数量 |
|------|-------------|----------|----------|
| fire | 76.6% | 216张 | 主导类别 |
| battery | 13.5% | 188张 | 少数类别 |
| board | 9.9% | 327张 | 少数类别 |

---

## 二、核心问题诊断

### 2.1 ⚠️ 预训练权重与模型架构不匹配（严重问题）

**问题描述**: 训练日志中出现大量 shape 不匹配警告：

```
The shape [24] in pretrained weight backbone.stages.0.blocks.0.conv1.bn._mean
is unmatched with the shape [48] in model backbone.stages.0.blocks.0.conv1.bn._mean.
```

**原因分析**:
- 预训练权重 `ppyoloe_crn_s_300e_coco.pdparams` 是标准 PP-YOLOE-s（channels=[32, 64, 128, 256, 512]）
- 但当前模型配置 `ppyoloe_crn.yml` 中 channels=[64, 128, 256, 512, 1024]，是 **PP-YOLOE-m** 的配置
- 这导致 **backbone 的大部分权重无法加载**，模型几乎是从头训练

**影响**: 预训练权重的迁移学习效果大打折扣，模型需要更多数据/epoch 才能收敛到同等水平。

**修复方案**:
```yaml
# 方案 A：使用正确的 PP-YOLOE-s 配置（推荐）
CSPResNet:
  layers: [3, 6, 6, 3]
  channels: [32, 64, 128, 256, 512]  # 改回 s 版本

# 方案 B：使用 PP-YOLOE-m 预训练权重
pretrain_weights: https://paddledet.bj.bcebos.com/models/ppyoloe_crn_m_300e_coco.pdparams
```

**预期提升**: +0.02~0.05 F1（正确迁移学习效果）

---

### 2.2 ⚠️ class_weight 配置未生效

**问题描述**: 配置文件中设置了 `class_weight: [1.5, 2.0, 0.8]`，但 PPYOLOEHead 的 `loss_weight` 参数只接受 dict 格式 `{class: 1.0, iou: 2.5, dfl: 0.5}`，列表格式不被识别。

**当前状态**: 依赖数据过采样解决类别不平衡，但效果有限。

**改进方案**: 使用 Focal Loss 或自定义 loss 权重，或通过更激进的过采样策略。

---

### 2.3 数据规模过小

**当前数据**: 405 张原始图片（324 训练 + 81 验证）

**问题**:
- 对于 3 类检测任务，数据量严重不足
- 即使经过过采样（584张）和数据增强，模型仍容易过拟合
- 验证集仅 81 张，评估结果方差较大

---

## 三、改进方案调研

### 3.1 模型层面改进

#### 方案 1：修复预训练权重匹配（优先级：🔴 最高）

**实施步骤**:
1. 确认当前 `ppyoloe_crn.yml` 中的 channels 配置
2. 如果使用 channels=[64,128,256,512,1024]，则换用 PP-YOLOE-m 预训练权重
3. 或者将 channels 改回 [32,64,128,256,512] 以匹配 PP-YOLOE-s

**预期效果**: F1 +0.02~0.05

#### 方案 2：升级到 PP-YOLOE+ 或 RT-DETR

**PP-YOLOE+ 改进点**:
- ET-Head（Efficient Task-aligned Head）
- CSPResNet 改进版
- 更好的 label assignment 策略

**RT-DETR 优势**:
- Transformer 架构，对小数据集更友好
- 端到端检测，无需 NMS 后处理
- PaddleDetection 官方支持

**实施难度**: 中等
**预期效果**: F1 +0.03~0.06

#### 方案 3：使用更大模型（PP-YOLOE-l/x）

**约束**: RTX 4060 8GB VRAM，batch_size 可能需要降到 2

**权衡**:
- 更大模型 → 更强表达能力 → 更好检测效果
- 但 batch_size 过小会影响 BN 统计和训练稳定性

**预期效果**: F1 +0.01~0.03

---

### 3.2 数据层面改进

#### 方案 4：更激进的数据扩充（优先级：🔴 高）

**4a. 增加训练数据**:
- 收集更多火焰/电池/指示牌图片
- 使用公开数据集补充（如 Fire Detection Dataset、COCO fire subset）
- 目标：至少 1000+ 张训练图片

**4b. 改进 Copy-Paste 策略**:
- 当前只提取了 3 个 hard negative crop（太少）
- 增加 hard negative 样本数量
- 对 board 类别进行更频繁的 copy-paste

**4c. 使用更强的离线增强**:
- 当前只用了 90° 旋转 + 水平翻转
- 可增加：180°/270° 旋转、随机裁剪、颜色抖动、MixUp 离线版

**预期效果**: F1 +0.02~0.04

#### 方案 5：改进类别平衡策略

**当前问题**: board 类别仅 9.9%，即使过采样到 327 张仍不足

**改进方案**:
- **Class-aware sampling**: 每个 batch 保证包含所有类别
- **Weighted loss（需修改代码）**: 在 VarifocalLoss 中添加 per-class 权重
- **更激进过采样**: board 从 2 份增加到 4-5 份

**预期效果**: F1 +0.01~0.02

---

### 3.3 训练策略改进

#### 方案 6：优化超参数（优先级：🟡 中）

**6a. 学习率调度优化**:
```yaml
# 当前：CosineDecay + LinearWarmup(5)
# 建议：尝试 CosineRestartDecay 或 StepDecay
schedulers:
  - !CosineDecay
    max_epochs: 200
  - !LinearWarmup
    start_factor: 0.0
    epochs: 10  # 增加 warmup 到 10 epochs
```

**6b. 增加训练轮数**:
- 当前 200 epochs 可能不够（特别是预训练权重不匹配时）
- 建议增加到 300-400 epochs
- 配合 early stopping 防止过拟合

**6c. 优化 NMS 参数**:
```yaml
# 当前
nms_threshold: 0.7
score_threshold: 0.03

# 建议：更保守的 NMS
nms_threshold: 0.6  # 降低 NMS 阈值，减少漏检
score_threshold: 0.05  # 提高置信度阈值，减少误检
```

**预期效果**: F1 +0.01~0.02

#### 方案 7：阈值优化

**当前状态**: 已有 `find_best_thresholds.py` 脚本

**改进方向**:
- 使用更细粒度的网格搜索（步长 0.05）
- 对每个类别独立优化阈值
- 使用验证集进行阈值选择，避免过拟合

**预期效果**: F1 +0.005~0.01

---

### 3.4 后处理改进

#### 方案 8：多尺度测试增强（TTA）

**原理**: 对同一图片在多个尺度下推理，合并结果

**实施**:
```python
# 在 predict.py 中添加 TTA 支持
scales = [0.8, 1.0, 1.2]
flips = [False, True]
# 对每个 (scale, flip) 组合推理，合并结果
```

**预期效果**: F1 +0.01~0.02（推理速度会下降）

#### 方案 9：模型集成

**方案**:
- 训练多个不同配置的模型
- 使用加权 NMS 或 Soft-NMS 合并结果
- 或使用不同 epoch 的 checkpoint 进行集成

**预期效果**: F1 +0.02~0.03（但需要更多训练时间和推理资源）

---

## 四、优先级排序与实施路线图

### 阶段一：快速修复 ✅ 已完成

| 优先级 | 方案 | 预期提升 | 实施难度 | 状态 |
|--------|------|----------|----------|------|
| 🔴 P0 | 修复预训练权重匹配 | +0.02~0.05 | 低 | ✅ 已修复 (depth/width_mult) |
| 🔴 P0 | 增加 hard negative 样本 | +0.01~0.02 | 中 | ✅ 已执行 (iou_threshold=0.3) |
| 🟡 P1 | A1 调参（epoch/NMS/warmup） | +0.01~0.03 | 低 | ✅ 已落地 (`ppyoloe_fire_a1.yml`) |
| 🟡 P1 | 更激进的过采样（board 4-5份） | +0.01~0.02 | 低 | ⏸ 待执行 |

### 阶段二：数据扩充

| 优先级 | 方案 | 预期提升 | 实施难度 | 时间 |
|--------|------|----------|----------|------|
| 🔴 P0 | 收集更多训练数据 | +0.02~0.04 | 高 | 1-3天 |
| 🟡 P1 | 改进离线增强策略 | +0.01~0.02 | 中 | 2小时 |
| 🟡 P1 | 优化 Copy-Paste 参数 | +0.005~0.01 | 中 | 1小时 |

### 阶段三：模型优化

| 优先级 | 方案 | 预期提升 | 实施难度 | 状态 |
|--------|------|----------|----------|------|
| 🔴 P0 | B1 RT-DETR-R18（并行试验） | +0.03~0.06 | 中 | ✅ 已落地 (`RT-DETR/`) |
| 🟡 P1 | 增加训练轮数到 300+ | +0.01~0.02 | 低 | ✅ 含于 A1 (epoch=350) |
| 🟡 P1 | 优化 NMS/阈值参数 | +0.005~0.01 | 低 | ✅ 含于 A1 (NMS=0.55, score=0.05) |
| 🟢 P2 | 升级到 PP-YOLOE+ | +0.03~0.06 | 中 | ⏸ 待执行 |
| 🟢 P2 | TTA 多尺度测试 | +0.01~0.02 | 中 | ⏸ 待执行 |

---

## 五、推荐实施顺序

### 第一步：修复预训练权重（立即执行）

这是影响最大的单一改进。当前配置导致预训练权重几乎完全浪费。

```bash
# 检查当前模型配置
cat mycode/configs/ppyoloe_crn.yml | grep channels

# 如果是 [64, 128, 256, 512, 1024]，下载 PP-YOLOE-m 权重
wget https://paddledet.bj.bcebos.com/models/ppyoloe_crn_m_300e_coco.pdparams
```

### 第二步：增加数据量（中期目标）

- 目标：将训练集从 324 张扩充到 1000+ 张
- 方法：收集公开数据集 + 改进 copy-paste 策略

### 第三步：调优训练配置（持续迭代）

- 增加训练轮数
- 优化 NMS 和阈值参数
- 尝试不同模型架构

---

## 六、预期效果总结

| 阶段 | 措施 | 预期 F1 | 累计提升 |
|------|------|---------|----------|
| 当前 | - | 0.80614 | - |
| 阶段一 | 修复权重 + 增加数据 + 过采样 | 0.83~0.85 | +0.02~0.04 |
| 阶段二 | 数据扩充 + 增强优化 | 0.85~0.87 | +0.04~0.06 |
| 阶段三 | 模型升级 + 集成 | 0.87~0.90 | +0.06~0.09 |

**最乐观预期**: 通过全部改进，F1 可达到 0.88~0.90

**最可能结果**: 通过阶段一和阶段二的改进，F1 可达到 0.85~0.87

---

## 七、风险与注意事项

1. **过拟合风险**: 数据量小，增强过强可能导致训练集和验证集分布不一致
2. **训练时间**: 增加 epoch 和数据量会显著增加训练时间（RTX 4060 约 4-6 小时/200 epochs）
3. **VRAM 限制**: 8GB VRAM 限制了 batch_size 和模型大小的选择
4. **评估偏差**: 验证集仅 81 张，F1 的置信区间较宽

---

## 八、附录：关键文件清单

| 文件 | 用途 | 修改建议 |
|------|------|----------|
| `mycode/configs/ppyoloe_fire.yml` | PP-YOLOE-s 训练配置（原始版） | 对照基线 |
| `mycode/configs/ppyoloe_fire_a1.yml` | PP-YOLOE-s A1 调参版 | 直接训练 |
| `RT-DETR/configs/rtdetr_r18vd_fire.yml` | RT-DETR-R18 训练配置 | 直接训练 |
| `mycode/calculate_f1.py` | F1 计算 | 无 |
| `mycode/find_best_thresholds.py` | 阈值优化 | 使用更细粒度搜索 |
| `mycode/hard_negative_mining.py` | Hard Negative 挖掘 | 增加提取数量 |
| `mycode/copy_paste_augmentation.py` | Copy-Paste 增强 | 优化融合参数 |

---

## 九、2026-06-11 更新：A1 + B1 方案落地

### 9.1 方案 A1：PP-YOLOE-s 调参优化

**变更明细**：

| 参数 | 原始 | A1 | 理由 |
|------|------|-----|------|
| epoch | 200 | 350 | 小数据集+强增强需更多迭代 |
| warmup_epochs | 5 | 12 | 配合 lr=0.01 需更长预热 |
| close_mosaic | 30 | 55 | 延长无 Mosaic 精调期 |
| nms_threshold | 0.7 | 0.55 | 降低阈值减少漏检 |
| score_threshold | 0.03 | 0.05 | 提高以减少低置信度 FP |
| GridMask prob | 0.4 | 0.5 | 加强正则化防过拟合 |

配置文件：`mycode/configs/ppyoloe_fire_a1.yml`
预期 F1：0.83~0.85

### 9.2 方案 B1：RT-DETR-R18

新建 `RT-DETR/` 方案目录，包括：
- `configs/rtdetr_r18vd_fire.yml`：完整训练配置（300 epochs, AdamW, lr=0.0001, batch_size=4）
- `README.md`：方案文档（架构对比、配置说明、训练步骤）
- `train.bat`：一键训练脚本

RT-DETR 优势：
- Transformer 端到端检测，无需 NMS 后处理
- HungarianMatcher 天然对 minority 类别（board/battery）友好
- DINOHead + VarifocalLoss 对类别不平衡有抑制
- ResNet18 backbone ~78MB，RTX 4060 8GB 可训练

预期 F1：0.84~0.88

### 9.3 相关文档更新

| 文件 | 更新内容 |
|------|----------|
| `README.md` | 更新训练配置表、新增 A1/B1 训练命令、添加优化路线图 |
| `Agent.md` | 新增 A1 变更明细 + B1 关键配置 + 训练命令 |
| `训练结果改进调研报告.md` | 本文件，新增第 9 章节 |

---

*报告生成时间: 2026-06-11（更新于同日）*
*数据来源: 项目日志、配置文件、代码分析*
